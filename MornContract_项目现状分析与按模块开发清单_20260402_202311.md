# MornContract 项目现状分析与按模块开发清单

- 生成时间：2026-04-02 20:23:11
- 分析依据：当前项目代码结构、`app/` 路由、`app/api/` 接口、`lib/` 业务服务与数据存储、当前页面实现、构建与测试结果
- 分析范围：以当前仓库代码为准，不以历史文档、旧版说明或过时需求稿为准

## 一、项目定位与目标

从当前代码判断，本项目不是单一的“合同编辑页”，而是一套面向企业与团队协作场景的 **AI 合同工作台 / 合同生成与签署平台**。系统目标是把“合同起草、模板复用、资料导入、合同管理、签署流转、支付订阅、后台运营”串成一个完整闭环。

结合现有代码，本项目需要完成的核心功能包括：

1. 用户身份体系与区域化部署能力
2. AI 合同生成、合同解析、导入与编辑能力
3. 合同模板库与模板驱动的生成流程
4. 合同列表、详情、版本、导出、分享、下载等文档工作台能力
5. 团队空间、成员邀请、协作与管理能力
6. 签署流转、待办、状态跟踪与公开验真能力
7. 订阅、支付、回调、账单与套餐治理能力
8. 管理后台中的用户、版本、广告位、系统设置、审计、统计分析等运营能力
9. 官网/营销页、帮助页、联系页、隐私协议、条款等对外页面
10. 工程侧的构建、测试、区域适配和部署支撑能力

## 二、当前项目技术与架构现状

### 1. 技术栈

- 前端框架：Next.js 15、React 19、TypeScript
- 路由形态：App Router
- 区域化支持：`CN` 与 `INTL` 双区域运行逻辑
- 中国区后端适配：CloudBase
- 国际区后端适配：Supabase
- AI 能力：OpenAI / Anthropic 适配已接入代码结构
- 支付能力：Stripe、PayPal、微信支付、支付宝适配层均已存在

### 2. 代码结构反映出的系统模块

当前 `app/` 目录已经覆盖：

- `admin`
- `api`
- `auth`
- `companies`
- `contact`
- `contracts`
- `create`
- `customers`
- `dashboard`
- `download`
- `features`
- `how-it-works`
- `invite`
- `mobile`
- `payment`
- `pricing`
- `privacy`
- `profile`
- `settings`
- `terms`
- `verify`
- `wechat`

当前 `app/api/` 目录已经覆盖：

- `admin`
- `ads`
- `auth`
- `company-info`
- `contracts`
- `dashboard`
- `geo`
- `health`
- `ocr`
- `payment`
- `profile`
- `public`
- `team-invites`

### 3. 数据与业务层能力

当前 `lib/data/` 与 `lib/` 业务层已经不是空壳，已形成较完整的数据组织：

- `contracts-store.ts`：合同 CRUD、合同主体数据管理
- `dashboard-store.ts`：仪表盘总览、模板、团队、账单等数据组织
- `dashboard-documents-store.ts`：文档列表、验真、证书、下载、分享等数据
- `workspace-documents-store.ts`：工作区文档管理、标签、组织、共享
- `workspace-invites-store.ts`：工作区邀请的创建、预览、接受、撤销
- `billing-store.ts`：支付记录与订阅数据
- `admin-insights-store.ts`：后台统计与分析数据
- `admin-management-store.ts`：后台用户、版本、文件上传等管理数据
- `admin-audit-store.ts`：审计日志、导出、汇总
- `admin-settings-store.ts`：系统设置读取与保存
- `payment-failure-notifications-store.ts`：支付失败通知队列持久化

## 三、当前代码已落地完成的功能

以下内容从当前代码判断，已经不只是“搭页面”，而是可以计入“已完成 / 已落地”的范围。

### 1. 认证与权限体系

已完成情况：

- 存在统一认证能力：`lib/auth/auth.ts`
- 存在客户端认证封装：`lib/auth/client.ts`
- 中国区 CloudBase 认证实现已存在：`lib/auth/cloudbase-auth.ts`
- 用户角色标准化与管理员角色判断已实现：`lib/auth/user-role.ts`
- 说明项目已经具备登录态校验、角色判断、区域化认证适配的基础能力

结论：

- **认证主链路已落地，可计入已完成模块**

### 2. AI 合同生成主流程

已完成情况：

- `lib/ai/contract-ai.ts` 已实现会话分析、合同生成、分析后生成等能力
- `lib/ai/contract-intake-chat.ts` 已实现合同采集式对话流程
- 当前项目同时具备“对话输入”“文本分析”“生成合同草稿”的服务层代码
- `/create`、`/create/analyze`、合同生成相关 API 已形成完整主流程
- 结合上一轮已完成改造，模板参数已经打通到创建与生成流程中

结论：

- **AI 合同生成主线已打通，属于本项目最核心的已落地能力之一**

### 3. 模板中心与模板驱动生成

已完成情况：

- Dashboard 侧已有模板中心能力
- 模板可进入创建流程
- `api/contracts/generate` 已保留并消费模板相关参数
- 模板到创建页、分析页、生成接口的链路已在当前代码中打通

结论：

- **模板库不再只是展示功能，已进入合同生成闭环**

### 4. 合同管理与文档工作台

已完成情况：

- 合同存储、列表、详情、基础 CRUD 已在 `contracts-store.ts` 中落地
- Dashboard 文档侧能力已在 `dashboard-documents-store.ts` 中组织完成
- 工作区文档管理能力已在 `workspace-documents-store.ts` 中落地
- 支持下载、验真、证书、分享、标签、归档等文档工作台相关能力

结论：

- **合同与文档管理模块已形成可用工作台，不再属于纯演示状态**

### 5. Dashboard 主工作台

已完成情况：

- Dashboard 总览、模板、团队、账单、文档等能力均有对应数据层
- `/dashboard/signatures` 当前已接入真实合同/签署流数据，不再是早期静态占位
- 工作台已覆盖核心业务入口，而不是单页展示

结论：

- **Dashboard 主工作台主体功能已落地**

### 6. 团队空间与邀请链路

已完成情况：

- `workspace-invites-store.ts` 已实现邀请创建、预览、接收、撤销
- 存在公开邀请 API：`app/api/public/team-invites/[token]/route.ts`
- 存在邀请落地页：`app/invite/workspace/[token]/page.tsx`

结论：

- **团队邀请与空间协作入口已经形成闭环**

### 7. 公开验真与证书能力

已完成情况：

- 存在公开文档验真接口：`app/api/public/documents/[token]/verify/route.ts`
- 存在证书接口：`app/api/public/documents/[token]/certificate/route.ts`
- 存在公开验真页面：`app/verify/document/[token]/page.tsx`

结论：

- **合同验真能力已具备对外可访问链路**

### 8. 支付、订阅与回调主链路

已完成情况：

- 存在统一支付适配层：`lib/payment/adapter.ts`
- 支付与订阅记录同步：`lib/payment/subscription-payment-sync.ts`
- Webhook 主处理器：`lib/payment/webhook-handler.ts`
- 多支付渠道适配代码已存在：Stripe / PayPal / 微信支付 / 支付宝
- 当前代码已经覆盖支付创建、支付确认、状态同步、历史账单等主链路
- 早期“支付失败通知逻辑未实现”的明确 TODO 已被清理，并已增加失败通知队列持久化

结论：

- **支付主链路基本已完成，已具备可计入交付范围的能力**

### 9. 管理后台主模块

已完成情况：

- 后台首页、分析页、用户管理、版本管理、设置页均已有实际页面实现
- `admin-settings-store.ts` 已具备设置读取与保存能力
- 后台统计、广告、审计、订阅等 API 结构完整
- 此前仅前端演示的系统设置页，目前已经接上真实数据读写能力

结论：

- **后台系统已经从“展示型后台”进入“可运营后台”阶段**

### 10. 公共页面与品牌页

已完成情况：

- `contact`、`pricing`、`features`、`how-it-works`、`privacy`、`terms` 等页面已经存在
- 联系页已从旧示例联系方式改为读取系统设置
- 品牌文案在核心区域已统一到 `MornContract`

结论：

- **公共页面体系已具备，但仍属于需要上线前持续打磨的区域**

### 11. 工程质量与可验证性

已完成情况：

- 当前项目 `npm run build` 已通过
- 当前项目测试已通过：11 个测试套件、30 个测试用例全部通过
- 已存在覆盖认证、签署流、支付、Dashboard、工作区邀请、治理等主路径的测试文件

测试覆盖到的代表性文件包括：

- `tests/auth-state-manager.test.ts`
- `tests/contract-draft-context.test.ts`
- `tests/contract-signflow-edgecases.test.ts`
- `tests/contract-signflow-mainline.test.ts`
- `tests/dashboard-management-mainline.test.ts`
- `tests/governance-mainline.test.ts`
- `tests/payment-confirm-route.test.ts`
- `tests/payment-governance-mainline.test.ts`
- `tests/payment-webhook-routes.test.ts`
- `tests/routing-governance-mainline.test.ts`
- `tests/workspace-invites-store.test.ts`

结论：

- **项目当前不是“只能看页面”的状态，而是已经具备构建可通过、测试可通过的工程基础**

## 四、当前未完成 / 半成品 / 待进一步闭环部分

这一部分是当前代码现状里最值得继续推进的内容。它们并非“完全没有做”，而是已经做到一定程度，但还不能全部算作最终完成。

### 1. 支付失败通知的“外发执行闭环”仍不够完整

现状：

- 当前已经存在支付失败通知队列持久化：`payment-failure-notifications-store.ts`
- Webhook 中也已接入失败事件处理入口
- 但从当前业务代码中，**尚未看到明确的真实外发执行器**，例如稳定的邮件发送服务、消息任务消费者、重试调度器或通知投递 Worker

结论：

- **支付失败事件“已能入队”，但“谁来真正发出去、如何重试、失败如何补偿”仍建议明确补齐**

### 2. 非核心营销页与公共文案仍有继续清理空间

现状：

- 核心业务页面已经明显比早期版本更完整
- 但在部分公共页面、营销表达、非核心说明区块中，仍可能看到偏通用、偏模板化或演示性质较强的文案
- 个别页面仍存在中英文混排风格不完全统一的问题

结论：

- **不影响核心业务使用，但上线前仍建议统一品牌语气、运营信息和对外文案**

### 3. 语言一致性与源码清洁度仍值得继续排查

现状：

- 当前核心模块已做过一轮清理
- 但从整个项目规模看，非核心文件中仍有可能残留历史文案、编码痕迹、语言风格不统一或内部注释表达不一致的问题

结论：

- **这属于“可上线前精修项”，不是主流程阻塞项，但对最终交付观感有影响**

### 4. 内部示例数据与演示痕迹尚未彻底清空

现状：

- `lib/db/index.ts` 一类的内部数据文件中，仍能看到示例邮箱、示例用户、演示型数据痕迹
- 这些内容多数属于内部种子数据，不直接影响主流程

结论：

- **内部 seed/demo 数据建议在正式发布前重新梳理，避免运营演示数据混入正式环境认知**

### 5. 工程维护项仍需持续推进

现状：

- 当前构建已经通过
- 但依赖维护层面仍存在类似 `baseline-browser-mapping` 过期提示这类问题
- 说明项目已进入“能交付，但仍需持续维护依赖与工程基线”的阶段

结论：

- **属于工程维护项，不是当前功能阻塞项**

## 五、按模块开发清单

以下按“已完成 / 进行中 / 建议下一步”进行整理，方便后续继续推进。

### 1. 认证与账户模块

- 当前状态：已完成主体能力
- 已完成：区域化认证适配、登录态校验、角色判断、管理员权限基础
- 建议下一步：继续补充异常态提示、一致化会话失效处理、补更多权限边界测试

### 2. AI 合同生成模块

- 当前状态：已完成主体能力
- 已完成：合同采集对话、文本分析、合同生成、模板参与生成链路
- 建议下一步：补更细粒度错误回退、生成失败重试与用户可见诊断

### 3. 模板中心模块

- 当前状态：已完成主流程
- 已完成：模板展示、模板选择、模板驱动创建、模板参数传递到生成链路
- 建议下一步：补模板分类治理、模板版本管理、模板质量运营工具

### 4. 合同管理与文档工作台模块

- 当前状态：已完成主体能力
- 已完成：合同 CRUD、详情、分享、下载、证书、验真、标签与组织能力
- 建议下一步：补更多批量操作、筛选体验、归档策略与长期留存规则

### 5. 签署工作台模块

- 当前状态：已完成主工作台能力
- 已完成：签署数据接入、状态汇总、签署流主展示
- 建议下一步：补更多异常态文案、空状态引导、催签和通知动作闭环

### 6. 团队与工作区模块

- 当前状态：已完成主链路
- 已完成：工作区邀请创建、公开邀请预览、接受、撤销
- 建议下一步：补团队角色细分、成员操作审计、更多协作治理规则

### 7. 支付与订阅模块

- 当前状态：主链路已完成，通知闭环待加强
- 已完成：多支付渠道适配、支付创建、回调、订阅与账单同步
- 建议下一步：补真实通知发送器、失败补偿、重试策略、人工介入机制

### 8. 后台管理模块

- 当前状态：已完成主体能力
- 已完成：后台首页、分析、用户、版本、设置、广告、审计等模块骨架与主数据能力
- 建议下一步：继续统一保存反馈、失败提示、空状态、批量操作确认与管理后台交互细节

### 9. 公共官网与营销页面模块

- 当前状态：已完成基础站点能力，仍需打磨
- 已完成：基础页面、品牌页、联系页、帮助信息页
- 建议下一步：统一品牌文案、替换泛化营销表达、梳理对外真实运营信息

### 10. 工程质量与测试模块

- 当前状态：可交付
- 已完成：构建通过、核心主路径测试覆盖
- 建议下一步：继续扩展集成测试、清理低价值依赖告警、建立发布前检查清单

## 六、开发进度报告

结合当前代码完成度，可以将项目进展整理为以下状态：

### 1. 总体进度判断

- 如果按“是否已经具备核心业务闭环”来评估，项目已经进入 **中后期 / 可交付迭代阶段**
- 如果按“是否达到正式上线前的精修标准”来评估，项目仍处于 **上线前打磨阶段**

### 2. 分模块进度判断

| 模块 | 当前状态 | 进度判断 |
| --- | --- | --- |
| 认证与权限 | 已落地 | 90% |
| AI 合同生成 | 已落地 | 88% |
| 模板中心 | 已打通主闭环 | 85% |
| 合同与文档管理 | 已落地 | 88% |
| Dashboard 工作台 | 已落地 | 86% |
| 签署工作台 | 主链路可用 | 82% |
| 团队与邀请 | 已落地 | 85% |
| 公开验真与证书 | 已落地 | 90% |
| 支付与订阅 | 主链路完成，通知闭环待补 | 80% |
| 管理后台 | 已从演示型进入可运营型 | 84% |
| 公共页面与营销站点 | 可用但需统一打磨 | 75% |
| 工程构建与测试 | 可交付 | 90% |

### 3. 当前整体完成度建议

综合判断：

- **核心业务完成度：约 85% 左右**
- **正式上线前完成度：约 78% 至 82%**

说明：

- 之所以核心业务完成度较高，是因为认证、AI 生成、模板链路、合同管理、支付主链路、后台主模块、公开验真等都已具备实际代码支撑
- 之所以正式上线前完成度略低，是因为外发通知闭环、文案统一、示例数据清理、后台交互细节和品牌一致性仍有收尾工作

## 七、建议的下一阶段开发优先级

### P0：优先补齐上线闭环

1. 补齐支付失败通知的真实发送执行器
2. 明确通知重试、失败补偿、人工介入机制
3. 统一后台保存成功/失败提示与空状态规范

### P1：统一上线前体验

1. 统一公共页面文案、营销页品牌表达、联系信息与对外口径
2. 继续清理历史注释、乱码、混合语言与演示痕迹
3. 清理内部 seed/demo 数据中的示例信息

### P2：工程与运营增强

1. 扩展更多集成测试与异常路径测试
2. 处理依赖维护类提醒与版本基线问题
3. 补管理后台批量管理与更完整的操作反馈

## 八、最终结论

基于当前项目代码，而不是历史文档，可以得出以下结论：

1. 本项目的目标已经非常明确，是一套面向企业合同场景的 AI 合同生成、管理、签署与运营平台
2. 当前代码已经完成了绝大多数核心业务主链路，尤其是认证、AI 合同生成、模板驱动生成、合同管理、公开验真、支付主链路、后台主模块
3. 项目当前已经不属于“原型阶段”，而属于 **可交付迭代阶段**
4. 目前最需要继续推进的不是从零开发核心模块，而是补齐少数闭环能力，并完成上线前统一打磨

## 九、附：本次分析的验证结果

- 构建验证：`npm run build` 已通过
- 测试验证：`npm test -- --runInBand` 已通过
- 测试结果：11/11 测试套件通过，30/30 测试用例通过

---

生成时间记录：2026-04-02 20:23:11
