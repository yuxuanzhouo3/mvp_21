# MornContract 项目现状分析与按模块开发清单

- 生成时间：2026-04-01 15:38:33 +08:00
- 分析范围：基于当前 `E:\xiangmu\code\new` 项目代码、页面、API、数据层、测试与迁移文件
- 分析原则：以“代码真实实现”为准，不以旧文档状态为准

## 1. 项目定位与目标判断

从当前代码看，这不是单一页面项目，而是一个“合同全生命周期平台”，核心目标已经很明确：

1. 面向中国区与国际区双部署。
2. 提供账号体系、地域分流、权限控制和安全治理。
3. 支持通过对话文本导入合同需求，AI 分析并生成合同草稿。
4. 支持合同的编辑、预览、导出、归档、签署流程跟踪。
5. 支持企业主体资料维护，以及营业执照 OCR 识别。
6. 支持控制台能力，包括合同中心、模板中心、团队成员、账单概览。
7. 支持支付与订阅管理，覆盖 Stripe、PayPal、微信支付、支付宝。
8. 支持管理后台，包括用户、订阅、审计、广告、版本、分析面板。

一句话概括：这是一个“带双区域部署、AI 合同生成、支付订阅、后台治理能力”的 SaaS 型合同平台。

## 2. 代码规模与总体判断

- 页面路由：约 54 个 `page.tsx`
- API 路由：约 60 个 `route.ts`
- 组件文件：约 112 个 `tsx`
- 业务/基础库文件：约 125 个 `ts/tsx`
- 自动化测试：5 个测试文件，12 个测试用例，当前全部通过

当前项目不是空壳，也不是只做了 UI。主线业务已经具备“页面 + API + 数据层 + 部分测试”的成型状态。

## 3. 当前实际完成度总评

### 3.1 总体进度

- 按“主线业务是否已经打通”估算：约 `78%`
- 如果把“截图 OCR 导入、文档存证/验真、所有签署入口统一、部分旧入口清理”都视为上线必备，则完整产品进度更接近 `68% - 72%`

### 3.2 当前最完整的主线

当前最完整、最可信的一条主线是：

`登录/鉴权 -> 文本导入 -> AI 分析 -> 创建合同草稿 -> 编辑 -> 导出 -> 合同详情 -> 签署流程跟踪`

这条线已经不是概念验证，而是有真实页面、真实 API、真实持久化与部分测试支撑。

## 4. 已完成功能清单

## 4.1 公共站点与路由治理

已完成：

1. 首页、功能页、价格页、隐私政策、条款、下载页、联系页等公开页面已存在。
2. 旧合同创建入口已经统一重定向到新主流程。
3. 中间件已实现受保护路由控制、管理员路由限制、API CORS 处理、请求体大小限制、地理区域识别、欧洲区拦截、CSRF 防护接入。

代码依据：

- `app/page.tsx`
- `app/features/page.tsx`
- `app/pricing/page.tsx`
- `app/privacy/page.tsx`
- `app/terms/page.tsx`
- `middleware.ts`
- `next.config.mjs`
- `tests/routing-governance-mainline.test.ts`

判断：

- 这部分已完成度高，属于可用状态。

## 4.2 双区域部署与环境切换

已完成：

1. 部署配置明确区分 `CN` 与 `INTL`。
2. 中国区与国际区分别切换认证、数据库、支付渠道。
3. 环境变量校验、启动安全检查、健康检查已存在。

代码依据：

- `lib/config/deployment.config.ts`
- `lib/config/region.ts`
- `lib/validation/env-validation.ts`
- `lib/monitoring/startup-checks.ts`
- `app/api/health/route.ts`

判断：

- 架构设计已完成。
- 真正上线是否稳定，仍依赖环境变量与外部服务配置是否齐全。

## 4.3 认证与账号体系

已完成：

1. 登录、注册、登出、刷新、当前用户信息、认证配置等 API 已存在。
2. 认证页同时支持中国区和国际区逻辑分流。
3. 中国区支持微信登录能力入口，国际区支持 Google / Supabase OAuth。
4. 密码安全、账户锁定、短信发送入口、解锁接口、会话管理、安全设置页面均已存在。
5. 控制台和后台均有独立的服务端鉴权封装。

代码依据：

- `app/auth/page.tsx`
- `app/api/auth/login/route.ts`
- `app/api/auth/register/route.ts`
- `app/api/auth/me/route.ts`
- `app/api/auth/config/route.ts`
- `lib/auth/auth.ts`
- `lib/auth/auth-utils.ts`
- `lib/auth/session-manager.ts`
- `app/settings/security/page.tsx`
- `app/settings/sessions/page.tsx`
- `lib/dashboard/server-auth.ts`
- `lib/auth/admin-auth.ts`

判断：

- 认证模块已完成主体能力。
- 中国区与国际区都不是“只有页面没有后端”，而是确实落到了不同后端适配上。

## 4.4 合同创建主流程

已完成：

1. 新建合同统一走 `/create` 主流程。
2. 第一步“导入方式选择”已完成。
3. 当前版本明确只开放“文本导入”。
4. 第二步“粘贴对话文本 -> 调用 AI 分析”已完成。
5. 分析结果会创建真实合同草稿，并写入数据库。
6. 第三步“AI 识别结果确认/人工修正”已完成。
7. 第四步“在线编辑 + 保存 + 下载 HTML”已完成。

代码依据：

- `app/create/page.tsx`
- `components/create/create-contract-screen.tsx`
- `app/create/import/page.tsx`
- `app/create/analyze/page.tsx`
- `app/create/edit/page.tsx`
- `app/api/contracts/analyze/route.ts`
- `app/api/contracts/generate/route.ts`
- `app/api/contracts/route.ts`
- `lib/contracts/client.ts`
- `lib/contracts/format.ts`
- `lib/data/contracts-store.ts`

判断：

- 这是项目当前最成熟的业务主线。
- 已经具备生产主线雏形。

## 4.5 AI 分析与合同生成

已完成：

1. AI 分析支持先做合同类型预判，再分配专家角色进行深入分析。
2. 合同生成支持根据分析结果生成结构化合同内容。
3. 中国区优先使用 DashScope/Qwen，国际区优先使用 OpenAI。
4. AI Prompt、合同类型、专家角色体系都已拆分成独立模块。

代码依据：

- `lib/ai/contract-ai.ts`
- `lib/ai/index.ts`
- `lib/ai/prompts/analyze.ts`
- `lib/ai/prompts/generate.ts`
- `lib/ai/prompts/experts.ts`
- `app/api/contracts/analyze/route.ts`
- `app/api/contracts/generate/route.ts`

判断：

- AI 不只是占位接口，而是有完整调用链。
- 真实可用性仍依赖 `OPENAI_API_KEY` 或 `DASHSCOPE_API_KEY` 等环境变量。

## 4.6 合同中心、详情、导出、归档、流程增强

已完成：

1. 合同列表 API 已支持分页、状态筛选、用户权限隔离。
2. 合同详情页可查看来源内容、版本历史、流程日志、签署证据、导出入口。
3. 支持 HTML / Word / PDF 导出。
4. 支持合同归档、恢复归档。
5. 支持签署流程状态推进，包括发起签署、发送提醒、双方确认、保留最终电子副本。

代码依据：

- `app/contracts/page.tsx`
- `app/contracts/[id]/page.tsx`
- `app/api/contracts/[id]/route.ts`
- `app/api/contracts/[id]/export/route.ts`
- `lib/contracts/enhancements.ts`
- `tests/contract-signflow-mainline.test.ts`
- `tests/governance-mainline.test.ts`

判断：

- 合同管理模块完成度高。
- 已不只是“生成合同”，而是已经扩展到“合同记录治理”。

## 4.7 移动端签署流程

已完成：

1. 存在移动端合同页、移动端签署页。
2. 手机端签署支持手写、输入、上传三种签名方式。
3. 会把签名证据、角色、方法、时间写回合同记录。
4. 可在移动端发起签署、记录对方确认、发送提醒、下载最终副本。

代码依据：

- `app/mobile/page.tsx`
- `app/mobile/contracts/page.tsx`
- `app/mobile/sign/page.tsx`
- `components/mobile/mobile-signature.tsx`

判断：

- 手机端签署不是假页面，是真正接入了合同 API。
- 这是项目的亮点之一。

## 4.8 企业资料与营业执照 OCR

已完成：

1. 企业资料支持多主体维护。
2. 支持设置当前激活企业主体。
3. 支持新增、编辑、删除企业资料。
4. 支持营业执照 OCR。
5. OCR 识别结果会回填企业名称、统一社会信用代码、法人、地址。
6. 识别前会对图片做压缩优化。
7. 同时存在“企业资料管理页”和“合同创建前的引导式企业设置页”。

代码依据：

- `app/companies/page.tsx`
- `app/contracts/company-setup/page.tsx`
- `app/api/company-info/route.ts`
- `app/api/ocr/business-license/route.ts`
- `lib/data/company-profile-store.ts`
- `lib/ocr/business-license.ts`
- `supabase/migrations/20260204_create_company_profiles.sql`
- `supabase/migrations/20260330_admin_audit_and_company_profile_governance.sql`

判断：

- 企业资料模块已经落库并且可用。
- OCR 不是本地伪解析，而是调用 AI 模型识图提取。

## 4.9 控制台 Dashboard

已完成：

1. Dashboard 总览页会真实拉取概览数据。
2. 控制台已有合同中心、模板中心、团队管理、账单页、签名页、文档页、设置页等模块。
3. 模板中心已支持模板列表、详情、创建、复制、版本分支、状态切换、使用计数。
4. 团队中心已支持邀请成员、修改角色、变更状态、删除成员，并区分 owner/admin/member 权限。
5. 账单页已接统一账单模型，显示计划、总支出、支付记录、待处理订单。
6. 总览统计会从合同、签署流程、支付记录中派生聚合数据。

代码依据：

- `app/dashboard/page.tsx`
- `app/dashboard/contracts/page.tsx`
- `app/dashboard/templates/page.tsx`
- `app/dashboard/team/page.tsx`
- `app/dashboard/billing/page.tsx`
- `app/api/dashboard/overview/route.ts`
- `app/api/dashboard/templates/route.ts`
- `app/api/dashboard/templates/[id]/route.ts`
- `app/api/dashboard/team/route.ts`
- `app/api/dashboard/team/[id]/route.ts`
- `app/api/dashboard/billing/route.ts`
- `lib/data/dashboard-store.ts`
- `tests/dashboard-management-mainline.test.ts`

判断：

- 这是另一条很完整的业务线。
- 模板库和团队管理都已经是真实能力，不是静态图。

## 4.10 支付、账单、订阅同步

已完成：

1. 支付中心页面已存在，并区分方案选择、支付、历史记录三个页签。
2. 支付方式会按区域切换。
3. 支持一次性支付创建接口。
4. 支持支付历史查询、继续支付、取消支付。
5. 支付记录与订阅记录之间存在统一同步模块。
6. 已实现 Stripe / PayPal / Alipay / WeChat webhook 入口。
7. webhook 到账后会同步 payment / subscription / 用户会员状态。
8. 已做重复支付拦截、状态同步、支付元数据标准化。

代码依据：

- `app/payment/page.tsx`
- `components/payment/payment-form.tsx`
- `components/payment/billing-history.tsx`
- `app/api/payment/onetime/create/route.ts`
- `app/api/payment/history/route.ts`
- `app/api/payment/continue/route.ts`
- `app/api/payment/cancel/route.ts`
- `app/api/payment/webhook/stripe/route.ts`
- `app/api/payment/webhook/paypal/route.ts`
- `app/api/payment/webhook/wechat/route.ts`
- `app/api/payment/webhook/alipay/route.ts`
- `lib/payment/subscription-payment-sync.ts`
- `lib/payment/payment-config.ts`
- `tests/payment-governance-mainline.test.ts`

判断：

- 支付不是空实现，后端闭环已具备。
- 但这个模块对外部配置依赖最高，实际可运行性取决于支付证书、密钥、Webhook 配置。

## 4.11 管理后台

已完成：

1. 后台总览页会拉真实统计。
2. 用户管理 API 已支持分页、搜索、订阅类型过滤。
3. 订阅管理支持查看订阅/支付数据并保存订阅状态。
4. 审计日志支持列表、汇总、CSV/JSON 导出。
5. 分析页支持趋势图、分布图、活跃度指标。
6. 广告位管理支持列表、指标、创建。
7. 版本管理、后台上传、后台设置页面均已存在。
8. 管理接口有独立管理员权限校验与审计记录。

代码依据：

- `app/admin/page.tsx`
- `app/admin/users/page.tsx`
- `app/admin/subscriptions/page.tsx`
- `app/admin/audit/page.tsx`
- `app/admin/analytics/page.tsx`
- `app/admin/ads/page.tsx`
- `app/admin/versions/page.tsx`
- `app/api/admin/stats/route.ts`
- `app/api/admin/users/route.ts`
- `app/api/admin/subscriptions/route.ts`
- `app/api/admin/audit/route.ts`
- `app/api/admin/analytics/route.ts`
- `app/api/admin/ads/route.ts`
- `app/api/admin/upload/route.ts`
- `lib/data/admin-management-store.ts`
- `lib/data/admin-insights-store.ts`
- `lib/data/admin-audit-store.ts`

判断：

- 后台能力较完整，已经具备“运营后台”雏形。

## 4.12 安全治理、审计、测试

已完成：

1. 环境变量校验与启动安全检查。
2. 中间件层面的访问控制、区域拦截、CSRF、防大请求体。
3. 登录失败锁定、密码强度校验。
4. 管理员操作审计能力。
5. 路由治理、合同治理、支付治理、Dashboard 治理已有自动化测试覆盖。
6. 当前测试结果全部通过。

代码依据：

- `middleware.ts`
- `lib/security/account-lockout.ts`
- `lib/security/password-security.ts`
- `lib/security/csrf.ts`
- `lib/data/admin-audit-store.ts`
- `tests/*.test.ts`

测试结果：

- 5 个测试套件通过
- 12 个测试用例通过

## 5. 部分完成或存在明显缺口的功能

## 5.1 创建入口中的截图导入、微信聊天直连尚未打通

现状：

1. `/create` 第一步里“上传截图”“选择微信对话”被明确标记为不可用。
2. 导入页也会把非文本方式强制提示为当前版本不可用。

代码依据：

- `components/create/create-contract-screen.tsx`
- `app/create/import/page.tsx`

结论：

- “文本导入”主线完成了。
- “截图导入”和“小程序聊天直连”仍属于待开发状态。

## 5.2 文档库、存证验真页面仍是静态演示数据

现状：

1. 文档库组件内部直接写死了文档数组。
2. 存储统计是固定数字。
3. 文档验真页面是静态展示，没有真实后端对接。

代码依据：

- `components/documents/document-library.tsx`
- `components/documents/storage-stats.tsx`
- `components/documents/document-verification.tsx`

结论：

- 这一整块更像“设计稿级实现”，不是已上线业务。

## 5.3 旧版网页签署入口仍是静态样例

现状：

1. `/dashboard/contracts/[id]/sign` 使用的 `SignatureInterface` 是硬编码合同内容。
2. 旧签署界面没有接入真实合同详情与真实签署回写。
3. 真实签署主线目前更偏向合同详情页流程增强和移动端签署页。

代码依据：

- `app/dashboard/contracts/[id]/sign/page.tsx`
- `components/contracts/signature-interface.tsx`

结论：

- 签署能力不是没有，而是“新旧入口并存，旧入口偏静态，移动端入口更真实”。

## 5.4 旧版 AI 对话式生成页仍是独立旧方案

现状：

1. `/contracts/ai-generate` 与 `/api/contracts/ai-chat` 是单独一套旧逻辑。
2. 这套逻辑以多轮对话收集劳动合同字段为主。
3. 生成结果没有和当前 `/create -> analyze -> edit` 主线完全统一。

代码依据：

- `app/contracts/ai-generate/page.tsx`
- `app/api/contracts/ai-chat/route.ts`

结论：

- 这是遗留能力，不是当前推荐主线。
- 后续应考虑合并或下线。

## 5.5 文档存储、区块链存证、合规验真尚未形成真实闭环

现状：

1. 页面设计了区块链哈希、验签、时间线、证书等展示。
2. 但从当前代码看，这部分没有与真实存证服务形成完整后端闭环。

代码依据：

- `components/documents/document-verification.tsx`
- `components/documents/document-library.tsx`

结论：

- 这是明确待开发模块。

## 5.6 部分实现依赖环境与外部服务，代码完成不等于可直接运行

高依赖模块：

1. AI 能力依赖 OpenAI / DashScope key。
2. OCR 依赖 OpenAI / DashScope 图像理解调用。
3. 国际区依赖 Supabase。
4. 中国区依赖 CloudBase。
5. 微信支付依赖商户号、私钥、序列号、API v3 key。
6. Stripe / PayPal / Alipay 均依赖生产配置和 webhook 正确回调。

结论：

- 代码结构已经具备。
- 部署环境不完整时，很多模块会退化或不可用。

## 6. 按模块开发清单

以下清单按优先级分为 P0、P1、P2。

## 6.1 P0：建议优先完成

### 模块 A：合同创建入口统一

待做事项：

1. 将 `/contracts/ai-generate` 旧流程与 `/create` 新流程统一。
2. 明确保留一个主入口，避免用户走到旧方案。
3. 统一合同创建后的记录结构、版本历史和跳转路径。

目标：

- 只保留一条标准创建主线。

### 模块 B：截图导入/OCR 导入正式接入创建流程

待做事项：

1. 把 `/create` 的 screenshot 方式从“禁用”改成真实可用。
2. 增加聊天截图 OCR 结构化解析。
3. 将 OCR 结果直接转成 `analysisResult` 或草稿字段。
4. 如需微信/飞书截图，增加来源识别和文本纠偏逻辑。

目标：

- 让“文本导入”和“截图导入”都进入同一合同草稿链路。

### 模块 C：网页签署入口统一到真实流程

待做事项：

1. 用真实合同数据替换 `SignatureInterface` 中的静态演示内容。
2. 将网页签署页和移动端签署页共用同一套签署状态机。
3. 统一提醒、确认、证据记录、最终电子副本保留逻辑。

目标：

- 桌面端和移动端签署体验一致，底层数据一致。

### 模块 D：文档中心从静态演示升级为真实模块

待做事项：

1. 为文档库建立真实表结构和上传接口。
2. 将合同导出结果接入文档库。
3. 将验证页接入真实文档哈希、签署证据和时间线。
4. 如果有区块链存证需求，补齐上链任务、回执记录和验证接口。

目标：

- 把“文档库/文档验真”从演示页面升级为真实资产中心。

## 6.2 P1：建议第二阶段完成

### 模块 E：支付闭环增强

待做事项：

1. 系统性回归 Stripe / PayPal / WeChat / Alipay 全链路。
2. 核对 webhook 幂等、失败补偿、退款、订阅取消、续费状态同步。
3. 统一一次性支付与订阅支付的数据模型展示。
4. 增加支付异常监控与管理后台手工修复工具。

目标：

- 从“代码可用”升级到“稳定运营”。

### 模块 F：企业资料与合同主体联动

待做事项：

1. 将当前激活企业主体自动带入合同生成。
2. 将企业 OCR 结果自动填入合同甲方信息。
3. 支持合同与主体的绑定关系查询、切换、历史留痕。

目标：

- 企业资料不只是单独管理页，而是成为合同生产资料。

### 模块 G：后台治理补强

待做事项：

1. 为后台页面补齐更多操作级测试。
2. 明确广告位、版本管理、上传中心的生产规则。
3. 为管理员修改用户/订阅等动作增加更细粒度审计字段。

目标：

- 后台从“可操作”提升到“可审计、可追责、可维护”。

## 6.3 P2：建议第三阶段优化

### 模块 H：体验和统一性优化

待做事项：

1. 统一旧入口与新入口命名。
2. 统一中英文文案和乱码问题。
3. 清理历史兼容逻辑和重复页面。
4. 增加更多端到端测试，而不仅是模块治理测试。

目标：

- 降低维护成本，提高整体一致性。

## 7. 按模块进度评估表

| 模块 | 当前状态 | 进度估算 | 说明 |
|---|---|---:|---|
| 公共站点与路由治理 | 已完成 | 90% | 页面、重定向、中间件治理都较完整 |
| 双区域部署与环境治理 | 已完成 | 85% | 架构完成，实际稳定性仍依赖外部配置 |
| 认证与账号体系 | 已完成 | 85% | 中国区/国际区双逻辑已经成型 |
| 合同创建主流程 | 已完成 | 80% | 文本导入主线已打通 |
| AI 分析与合同生成 | 已完成 | 80% | 调用链完整，但依赖外部模型服务 |
| 合同中心/详情/导出/归档 | 已完成 | 85% | 数据、导出、详情、流程增强已具备 |
| 移动端签署 | 已完成 | 75% | 已接真实 API，但桌面端入口尚未统一 |
| 企业资料与 OCR | 已完成 | 80% | 多主体与 OCR 已可用 |
| Dashboard 控制台 | 已完成 | 85% | 总览、模板、团队、账单都较完整 |
| 支付与订阅同步 | 部分完成 | 75% | 后端闭环有了，但强依赖真实支付配置 |
| 管理后台 | 已完成 | 80% | 已具备运营后台雏形 |
| 文档库与验真 | 部分完成 | 20% | 主要还是静态演示 |
| 旧版 AI 对话生成入口 | 部分完成 | 40% | 独立旧方案，未和新主线彻底统一 |
| 安全治理与测试 | 已完成 | 85% | 有基础测试和安全机制，但仍缺 E2E |

## 8. 当前代码里最值得肯定的部分

1. 主线不是只有页面，很多模块已经真正落到数据库与 API。
2. 双区域部署思路清晰，结构上已经能区分中国区和国际区。
3. 合同创建主流程已经可用，且有版本历史、导出、签署流程增强。
4. Dashboard 模块完整度很高，尤其模板中心和团队管理。
5. 支付与订阅同步模型已经做得比较系统。
6. 后台具备统计、审计、用户、订阅等治理能力。

## 9. 当前最主要的风险点

1. 文档库/验真/存证模块仍未真实落地，容易让人误判为已上线。
2. 旧入口与新入口并存，增加维护成本和用户困惑。
3. 支付虽然代码量大，但真实运行非常依赖环境配置。
4. OCR 与 AI 服务同样依赖第三方接口，缺少离线兜底。
5. 桌面签署页与移动签署页没有完全统一。
6. 页面中存在少量历史文案乱码，发布前需要统一校对。

## 10. 结论

基于当前代码，而不是旧文档，可以得出结论：

1. 这是一个已经完成大半的 SaaS 合同平台，不是初始脚手架。
2. 核心业务主线已经成立，尤其是“文本导入 -> AI 分析 -> 草稿生成 -> 编辑导出 -> 合同详情/签署流程”。
3. Dashboard、企业资料、后台治理、支付同步都已经具备较强雏形。
4. 当前最主要缺口集中在“截图导入接入主线”“文档中心真实化”“旧入口统一”“签署入口统一”“支付生产化验证”。
5. 如果下一阶段按 P0 清单推进，这个项目会很快从“功能成型”进入“可稳定交付”阶段。

## 11. 本次核查所参考的核心代码文件

- `middleware.ts`
- `next.config.mjs`
- `app/auth/page.tsx`
- `app/create/page.tsx`
- `app/create/import/page.tsx`
- `app/create/analyze/page.tsx`
- `app/create/edit/page.tsx`
- `app/contracts/[id]/page.tsx`
- `app/companies/page.tsx`
- `app/contracts/company-setup/page.tsx`
- `app/dashboard/page.tsx`
- `app/dashboard/templates/page.tsx`
- `app/dashboard/team/page.tsx`
- `app/dashboard/billing/page.tsx`
- `app/payment/page.tsx`
- `app/admin/page.tsx`
- `app/api/contracts/*.ts`
- `app/api/dashboard/**/*.ts`
- `app/api/payment/**/*.ts`
- `app/api/admin/**/*.ts`
- `lib/ai/**/*.ts`
- `lib/contracts/**/*.ts`
- `lib/data/**/*.ts`
- `lib/payment/**/*.ts`
- `lib/ocr/business-license.ts`
- `tests/*.test.ts`

