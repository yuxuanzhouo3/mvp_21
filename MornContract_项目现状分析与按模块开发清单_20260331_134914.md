# MornContract 项目现状分析与按模块开发清单

- 生成时间：2026-03-31 13:49:14 +08:00
- 分析基线：以当前仓库代码为准，不以旧文档、旧报告或历史计划为准
- 分析范围：`app/*` 页面路由、`app/api/*` 接口、`components/*` 关键组件、`lib/*` 数据层与治理层、`tests/*` 测试、`next.config.mjs`

---

## 1. 根据当前代码，本项目要完成什么功能

从现有页面、接口和数据层可以明确看出，这个项目的目标并不是单一“合同编辑器”，而是一套带国内版 / 国际版双数据后端的在线合同与签署平台，核心功能包括：

### 1.1 官网与获客转化

当前存在完整官网与营销页体系：

- 首页：`app/page.tsx`
- 功能页：`app/features/page.tsx`
- 定价页：`app/pricing/page.tsx`
- 客户案例：`app/customers/page.tsx`
- 联系我们：`app/contact/page.tsx`
- 使用说明：`app/how-it-works/page.tsx`
- 下载页：`app/download/page.tsx`
- 微信页：`app/wechat/page.tsx`
- 隐私与条款：`app/privacy/page.tsx`、`app/terms/page.tsx`

结论：项目目标包含完整产品官网、功能介绍、转化入口和品牌对外展示。

### 1.2 认证与账号体系

当前存在完整认证相关页面和 API：

- 页面：`app/auth/page.tsx`、`app/login/page.tsx`、`app/signup/page.tsx`、`app/auth/callback/page.tsx`
- API：`app/api/auth/*`
- 认证能力覆盖：邮箱密码、短信、手机号、微信、CloudBase 微信、登录状态、刷新、用户资料、密码更新等

结论：项目目标包含完整登录注册、回调、地区化认证和用户身份管理。

### 1.3 用户资料、设置与会话安全

当前存在：

- 用户资料页：`app/profile/page.tsx`
- 设置页：`app/settings/page.tsx`
- 安全设置页：`app/settings/security/page.tsx`
- 会话管理页：`app/settings/sessions/page.tsx`
- 统一控制中心组件：`components/account/account-control-center.tsx`
- 用户资料 API：`app/api/profile/route.ts`

结论：项目目标包含头像上传、通知偏好、安全设置、密码更新、会话撤销、会员状态展示和企业资料关联。

### 1.4 企业资料与 OCR 识别

当前存在：

- 企业资料页：`app/companies/page.tsx`
- 公司配置页：`app/contracts/company-setup/page.tsx`
- 企业资料 API：`app/api/company-info/route.ts`
- OCR API：`app/api/ocr/business-license/route.ts`
- 数据层：`lib/data/company-profile-store.ts`

结论：项目目标包含多主体企业资料管理、OCR 识别、截图文字提取、上传压缩和合同前置企业资料准备。

### 1.5 合同创建、AI 分析与合同管理

当前存在：

- 主创建入口：`app/create/page.tsx`
- 导入页：`app/create/import/page.tsx`
- 分析页：`app/create/analyze/page.tsx`
- 编辑页：`app/create/edit/page.tsx`
- 合同列表页：`app/contracts/page.tsx`
- 合同详情页：`app/contracts/[id]/page.tsx`
- AI 生成页：`app/contracts/ai-generate/page.tsx`
- 合同 API：`app/api/contracts/*`

结论：项目目标包含合同创建、导入、分析、编辑、详情、搜索筛选、归档、导出、删除和 AI 交互辅助。

### 1.6 电子签署与证据留存

当前存在：

- 合同详情页内签署流
- Dashboard 合同签署页：`app/dashboard/contracts/[id]/sign/page.tsx`
- 移动端签署页：`app/mobile/sign/page.tsx`
- 合同增强逻辑：`lib/contracts/enhancements.ts`
- 合同详情 API 更新动作：`app/api/contracts/[id]/route.ts`

结论：项目目标包含发起签署、双方确认、提醒、签署证据、最终电子版留存和操作日志。

### 1.7 Dashboard 工作台

当前存在：

- 首页：`app/dashboard/page.tsx`
- 合同页：`app/dashboard/contracts/page.tsx`
- 模板页：`app/dashboard/templates/page.tsx`
- 团队页：`app/dashboard/team/page.tsx`
- Billing 页：`app/dashboard/billing/page.tsx`
- 文档页：`app/dashboard/documents/page.tsx`
- 签署页：`app/dashboard/signatures/page.tsx`
- 设置页：`app/dashboard/settings/page.tsx`
- 对应 API：`app/api/dashboard/*`

结论：项目目标包含控制台首页、模板中心、团队协作、合同工作台、账单概览和文档/签署工作台。

### 1.8 支付与订阅

当前存在：

- 主支付页：`app/payment/page.tsx`
- 支付跳转/成功/取消/微信二维码页：`app/payment/*`
- 支付 API：`app/api/payment/*`
- 支付适配器与订阅同步逻辑：`lib/payment/*`

结论：项目目标包含国内外支付、订阅、一次性支付、支付状态确认、历史记录、Webhook 回调与账单管理。

### 1.9 后台管理与审计治理

当前存在：

- 后台首页：`app/admin/page.tsx`
- 用户管理：`app/admin/users/page.tsx`
- 订阅管理：`app/admin/subscriptions/page.tsx`
- 广告位管理：`app/admin/ads/page.tsx`
- 数据分析：`app/admin/analytics/page.tsx`
- 审计日志：`app/admin/audit/page.tsx`
- 版本管理：`app/admin/versions/page.tsx`
- 后台设置：`app/admin/settings/page.tsx`
- 对应 API：`app/api/admin/*`

结论：项目目标包含后台运营、用户/订阅/广告/版本管理、统计分析、上传管理、审计导出与管理员权限控制。

### 1.10 移动端访问与移动签署

当前存在：

- 移动端首页：`app/mobile/page.tsx`
- 移动端合同列表：`app/mobile/contracts/page.tsx`
- 移动端签署页：`app/mobile/sign/page.tsx`
- 组件：`components/mobile/mobile-contract-list.tsx`、`components/mobile/mobile-signature.tsx`

结论：项目目标包含移动端合同查看、签署跟进和移动场景操作。

### 1.11 国内版 / 国际版双后端统一

当前代码明显支持：

- 国内版：CloudBase
- 国际版：Supabase
- 区域判断：`lib/config/region`
- 统一数据模型：`lib/data/unified-models`
- 初始化脚本：`scripts/init-unified-governance.mjs`

结论：项目目标包含国内外双部署、双数据库实现、统一字段规范和统一业务行为。

---

## 2. 当前代码里已经完成了什么功能

下面的判断以“代码是否已经形成真实页面 + 真实 API + 真实数据层闭环”为标准。

### 2.1 官网与公开页面：已完成主体建设

已完成内容：

- 官网页面路由完整。
- 已具备功能介绍、定价、客户案例、联系入口、下载、微信页、隐私和条款页。
- 首页已不是空壳，具备产品化入口。

判断：官网与转化层已经成型。

### 2.2 认证体系：已完成主体建设

代码依据：

- `app/auth/page.tsx`
- `app/api/auth/login/route.ts`
- `app/api/auth/register/route.ts`
- `app/api/auth/refresh/route.ts`
- `app/api/auth/wechat/route.ts`
- `app/api/auth/cloudbase-wechat/route.ts`
- `app/api/auth/sms/send/route.ts`

已完成内容：

- 邮箱 / 密码登录注册。
- 认证状态、刷新、退出、用户信息获取。
- 短信与手机号能力。
- 微信相关认证链路。
- 国内版 / 国际版区域化逻辑分流。

判断：认证能力已经是正式业务模块，不是 demo。

### 2.3 用户资料与设置中心：已完成主体建设

代码依据：

- `app/profile/page.tsx`
- `app/settings/page.tsx`
- `app/settings/security/page.tsx`
- `app/settings/sessions/page.tsx`
- `components/account/account-control-center.tsx`
- `app/api/profile/route.ts`

已完成内容：

- 头像上传与压缩。
- 通知偏好设置。
- 安全设置切换。
- 密码更新。
- 当前会话与其它会话撤销。
- 统一会员状态展示。
- 企业资料入口与活跃企业资料绑定。

判断：用户资料、安全设置、会话管理已经基本成型。

### 2.4 企业资料：已完成主体建设

代码依据：

- `app/companies/page.tsx`
- `app/api/company-info/route.ts`
- `app/api/ocr/business-license/route.ts`
- `lib/data/company-profile-store.ts`

已完成内容：

- 企业资料列表与编辑。
- 多主体公司切换。
- 企业资料保存、删除。
- OCR 识别营业执照。
- OCR 来源支持营业执照、微信截图、飞书截图。
- 上传前压缩策略。
- OCR 失败后重试。
- 当前活跃企业写回用户资料。

判断：企业资料模块已经明显不是静态页，核心业务已接上真实数据层。

### 2.5 合同创建与合同管理：已完成主体建设

代码依据：

- `app/create/page.tsx`
- `app/create/import/page.tsx`
- `app/create/analyze/page.tsx`
- `app/create/edit/page.tsx`
- `app/contracts/page.tsx`
- `app/contracts/[id]/page.tsx`
- `app/api/contracts/route.ts`
- `app/api/contracts/[id]/route.ts`
- `app/api/contracts/[id]/export/route.ts`

已完成内容：

- 合同创建主流程已收口到 `/create`。
- 合同列表支持搜索、筛选、排序。
- 合同详情支持查看、更新、删除。
- 支持归档 / 取消归档。
- 支持导出 HTML / Word，并支持 PDF 场景下的 HTML 文档渲染输出。
- 合同详情页已接入操作日志与签署增强数据。
- AI 分析 / AI 生成 / AI 聊天接口存在。

判断：合同主链路已经成型，列表、详情、导出、归档、操作日志都已具备。

### 2.6 电子签署：Web 主链路已完成主体建设

代码依据：

- `app/contracts/[id]/page.tsx`
- `app/dashboard/contracts/[id]/sign/page.tsx`
- `app/api/contracts/[id]/route.ts`
- `lib/contracts/enhancements.ts`
- `tests/contract-signflow-mainline.test.ts`

已完成内容：

- 发起签署。
- 发起方确认。
- 对方确认。
- 提醒发送。
- 证据数据写入。
- 最终电子版留存。
- 操作日志记录。

判断：电子签署核心状态机和数据留痕已经在主链路中落地。

### 2.7 Dashboard 首页：已完成真实数据化

代码依据：

- `app/dashboard/page.tsx`
- `components/dashboard/dashboard-stats.tsx`
- `components/dashboard/recent-activity.tsx`
- `app/api/dashboard/overview/route.ts`
- `lib/data/dashboard-store.ts`

已完成内容：

- Dashboard 统计卡片已接真实数据。
- 最近活动列表已接真实数据。
- 数据来自合同总量、待签署、完成量、参与方活跃数和合同操作日志聚合。

判断：Dashboard 首页已脱离静态数组。

### 2.8 模板中心：已完成较完整的模板管理能力

代码依据：

- `app/dashboard/templates/page.tsx`
- `app/api/dashboard/templates/route.ts`
- `app/api/dashboard/templates/[id]/route.ts`
- `lib/data/dashboard-store.ts`

已完成内容：

- 模板列表读取数据库。
- 模板创建。
- 模板编辑。
- 模板复制。
- 模板新版本创建。
- 模板启用 / 归档状态切换。
- 从模板跳转进入创建流程。
- 模板状态与版本统计。

判断：模板中心已经不是“展示页”，而是一个轻中度完整的模板管理模块。

### 2.9 团队协作：已完成真实成员管理闭环

代码依据：

- `app/dashboard/team/page.tsx`
- `app/api/dashboard/team/route.ts`
- `app/api/dashboard/team/[id]/route.ts`
- `lib/data/dashboard-store.ts`

已完成内容：

- 成员列表读取数据库。
- 邀请成员。
- 角色变更。
- 状态变更。
- 移除成员。
- owner / admin / member 权限差异控制。

判断：团队协作页面已经不是静态成员演示，而是接上了真实成员管理流。

### 2.10 Dashboard Billing：已完成真实数据化

代码依据：

- `app/dashboard/billing/page.tsx`
- `app/api/dashboard/billing/route.ts`
- `lib/data/dashboard-store.ts`

已完成内容：

- 订阅概览读取真实订阅数据。
- 最近支付记录读取真实支付数据。
- 展示总支出、支付数量、会员到期时间、最近支付时间。
- 与主支付中心 `/payment` 建立跳转联动。

判断：Dashboard Billing 已经不是静态页，但它更偏控制台汇总视图。

### 2.11 主支付模块：已完成主体建设

代码依据：

- `app/payment/page.tsx`
- `app/api/payment/create/route.ts`
- `app/api/payment/confirm/route.ts`
- `app/api/payment/status/route.ts`
- `app/api/payment/history/route.ts`
- `app/api/payment/verify/route.ts`
- `app/api/payment/cancel/route.ts`
- `app/api/payment/onetime/*`
- `app/api/payment/webhook/*`

已完成内容：

- 支付创建。
- 支付确认与状态查询。
- 支付历史。
- 取消支付。
- 一次性支付接口。
- Stripe / PayPal / 微信 / 支付宝相关 webhook。
- 订阅支付字段同步。

判断：主支付中心已经是完整业务模块，不是简单占位页面。

### 2.12 后台管理：已完成主体建设

代码依据：

- 页面：`app/admin/page.tsx`、`app/admin/users/page.tsx`、`app/admin/subscriptions/page.tsx`、`app/admin/ads/page.tsx`、`app/admin/analytics/page.tsx`、`app/admin/audit/page.tsx`、`app/admin/versions/page.tsx`
- API：`app/api/admin/*`
- 管理员校验：`lib/auth/admin-auth.ts`

已完成内容：

- 后台首页统计。
- 用户管理 API。
- 订阅与支付后台管理。
- 广告位管理。
- 分析页。
- 审计日志页与导出。
- 上传管理 API。
- 版本管理 API。
- 管理员鉴权与拒绝访问审计。

判断：后台系统已经不是壳，且管理员权限校验和审计能力已经落地。

### 2.13 数据统一化：已完成基础设施建设

代码依据：

- `lib/data/unified-models`
- `scripts/init-unified-governance.mjs`
- `docs/UNIFIED_DATA_MODEL.md`
- `supabase/*.sql`

已完成内容：

- 用户 / 合同 / 支付 / 企业资料统一模型已存在。
- CloudBase 与 Supabase 双实现都在数据层中体现。
- 迁移与初始化脚本已存在。

判断：统一数据模型与迁移基础设施已经开始落地，而不是停留在文档层。

### 2.14 测试：已完成基础主线校验

当前测试文件：

- `tests/contract-signflow-mainline.test.ts`
- `tests/dashboard-management-mainline.test.ts`
- `tests/governance-mainline.test.ts`
- `tests/routing-governance-mainline.test.ts`

已覆盖内容：

- 合同签署主流程状态机。
- Dashboard 模板与团队权限。
- 审计汇总与 CSV 导出。
- 统一模型规范化。
- legacy 路由收口验证。

判断：已经有主线治理测试，但覆盖面仍偏窄。

---

## 3. 当前代码里仍是半完成或待继续开发的部分

### 3.1 移动端合同与签署：仍是最明显的未完全收口区

代码依据：

- `components/mobile/mobile-contract-list.tsx`
- `components/mobile/mobile-signature.tsx`

现状：

- 移动端合同列表已经从真实合同数据读取，不再是纯静态数组。
- 但页面仍存在大量历史乱码中文。
- 移动端签署页虽然接入真实合同读取与签署动作接口，但“手写 / 输入 / 上传”仍主要是前端展示式 UI。
- 签名素材本身没有看到完整文件上传、真实签名图片持久化或证据文件落库闭环。

判断：移动端已接入主数据流，但仍偏“移动壳 + 演示交互”，未完全产品化。

### 3.2 Dashboard Billing：更偏控制台汇总视图

代码依据：

- `app/dashboard/billing/page.tsx`

现状：

- 可以展示真实订阅和支付摘要。
- 可以跳转主支付中心继续支付。
- 但完整支付操作仍然主要在 `/payment`。

判断：Billing 页是控制台视图，不等同于完整支付业务前台。

### 3.3 合同导出中的 PDF 能力仍是 HTML 文档输出思路

代码依据：

- `app/api/contracts/[id]/export/route.ts`

现状：

- `format=word` 已输出可下载文档。
- `format=html` 已输出可下载 HTML。
- `format=pdf` 当前返回 `text/html` inline，更像打印 / 浏览器另存为 PDF 的支持，而不是服务端二进制 PDF 渲染。

判断：PDF 下载体验可用，但如果目标是严格意义的服务端 PDF 文件导出，这里仍可继续增强。

### 3.4 后台“数据口径验收”仍需业务层复核

代码依据：

- `app/api/admin/stats/route.ts`
- `app/api/admin/analytics/route.ts`
- `app/api/admin/users/route.ts`
- `app/api/admin/subscriptions/route.ts`
- `app/api/admin/ads/route.ts`

现状：

- 页面和 API 已经存在，且走真实数据。
- 但“真实数据是否和运营口径完全一致”属于业务验收问题，仅从代码无法直接证明已完全验收。

判断：后台功能已建成，但数据口径仍建议继续核对。

### 3.5 数据治理已经开始，但还没有彻底收口

现状：

- 已有统一模型。
- 已有国内外双实现。
- 已有迁移脚本。
- 但仓库中仍有一些历史计划文档、历史分析产物、兼容注释与乱码痕迹。

判断：数据治理已启动且已有成果，但代码洁净度还没完全收口。

---

## 4. 当前代码里暴露出的治理问题

### 4.1 质量门禁还没有完全恢复

代码依据：

- `next.config.mjs`

当前配置：

- `typescript.ignoreBuildErrors = true`

这说明：

- 当前项目虽然可以构建。
- 但不能等同于类型问题已经清零。
- 类型门禁仍处于放开状态。

补充说明：

- 当前没有再看到 `eslint.ignoreDuringBuilds = true`。
- 这意味着旧报告中“同时放开 ESLint 和 TypeScript”这一结论已经过时，现状应修正为“仅 TypeScript 仍被放开”。

### 4.2 仍存在历史乱码与中英文不一致

代码依据：

- `components/mobile/mobile-contract-list.tsx`
- `components/mobile/mobile-signature.tsx`
- `app/payment/page.tsx`
- `app/admin/page.tsx`
- `app/admin/subscriptions/page.tsx`
- `app/admin/ads/page.tsx`
- `app/admin/audit/page.tsx`

现状：

- 多个页面仍有 mojibake 文案。
- 一些中文与英文文案风格不一致。
- 少数注释也存在乱码。

判断：这是当前最直观、最影响感知质量的问题之一。

### 4.3 legacy 入口已经收口，但兼容逻辑仍保留

代码依据：

- `next.config.mjs`
- `lib/contracts/legacy-entrypoints.ts`
- `tests/routing-governance-mainline.test.ts`

现状：

- `/contracts/create`
- `/contracts/new`
- `/contracts/upload-template`

这些旧入口已通过 redirect 收口到新主流程，不再是并行页面。

判断：

- 这部分比旧报告中的情况更好。
- 但兼容映射仍保留，后续可以视实际流量决定是否继续保留。

### 4.4 测试覆盖不足

现状：

- 当前仅有 4 个主线测试文件。
- 尚未看到合同列表 / 详情 / 企业资料 / 支付主链路 / 后台多页面的更完整测试集。

判断：已有测试基础，但离“稳定交付级”还不够。

### 4.5 仓库根目录中仍有较多历史分析与计划文件

现状：

- 根目录中存在多个历史 `*.md` 报告文件和计划文件。
- 这不一定是错误，但会增加仓库噪音。

判断：文档沉淀有价值，但建议区分“正式文档”和“阶段性分析产物”。

### 4.6 `__MACOSX` 目录当前未发现

和旧报告不同，当前代码仓库中没有扫描到 `__MACOSX` 目录。

判断：这条历史问题在当前仓库现状下应视为已清理，不应继续按“仍存在”描述。

---

## 5. 当前项目整体完成度判断

如果从“主产品是否已经成型”来看，答案是：

**已经成型。**

如果从“是否已经完全收口并达到稳定交付状态”来看，答案是：

**还没有完全收口。**

更准确的判断是：

- 官网、认证、账号中心、企业资料、合同创建、合同管理、电子签署、Dashboard、主支付模块、后台系统，都已经完成主体建设。
- 模板中心、团队协作、Dashboard 首页和 Billing 也已经明显从静态展示走向真实数据驱动。
- 当前最主要的后续工作，不再是“从 0 到 1 搭功能”，而是“移动端真实化、乱码清理、质量门禁恢复、测试补齐、数据口径验收、历史兼容收口”。

---

## 6. 按模块拆解的开发清单

| 模块 | 当前状态 | 已完成内容 | 待完成 / 建议继续做 | 优先级 |
| --- | --- | --- | --- | --- |
| 官网与营销页 | 已完成主体建设 | 官网、功能、定价、客户、联系、下载、条款、隐私页已齐 | 优化文案、SEO、转化埋点与国际化细节 | P2 |
| 认证体系 | 已完成主体建设 | 邮箱、短信、手机号、微信、状态、刷新、回调、资料接口已具备 | 继续清理历史兼容接口与异常提示一致性 | P1 |
| 用户资料与设置中心 | 已完成主体建设 | 头像上传、通知偏好、安全设置、密码更新、会话管理、会员展示已具备 | 统一文案、继续补安全审计和异常反馈 | P1 |
| 企业资料 | 已完成主体建设 | 多主体切换、OCR、截图识别、上传压缩、失败重试、资料 CRUD 已具备 | 增加更细的 OCR 结果校正、失败分类和更多测试 | P1 |
| 合同创建主流程 | 已完成主体建设 | `/create` 主流程、导入、分析、编辑、AI 相关入口已具备 | 继续统一少量兼容入口、补全创建链路测试 | P1 |
| 合同列表与详情 | 已完成主体建设 | 搜索、筛选、排序、详情、归档、删除、导出、日志已具备 | 增加更完整的 E2E 测试与导出体验优化 | P1 |
| 电子签署 Web | 已完成主体建设 | 发起签署、双方确认、提醒、证据、最终留存已具备 | 继续增强签署证据展示与更多异常分支测试 | P1 |
| Dashboard 首页 | 已完成真实数据化 | 统计卡片和最近活动已接真实数据 | 继续优化展示文案和边界态 | P2 |
| 模板中心 | 已完成较完整能力 | 列表、创建、编辑、复制、版本、启用/归档、使用模板已具备 | 如果要做企业级治理，可继续补模板详情沉淀、审批/发布策略 | P1 |
| 团队协作 | 已完成真实管理闭环 | 邀请、角色变更、状态变更、移除成员、权限控制已具备 | 可继续补邀请接受流程、邮件通知、审计关联 | P1 |
| Dashboard Billing | 已完成真实数据化 | 订阅概览、支付摘要、最近支付已接真实数据 | 保持控制台定位，复杂支付动作继续集中到 `/payment` | P2 |
| 主支付模块 | 已完成主体建设 | 创建、确认、状态、历史、取消、一次性支付、Webhook 已具备 | 清理乱码、增强支付失败回滚和主链路测试 | P1 |
| 移动端合同列表 | 半完成 | 已接真实合同数据，支持查看、下载、分享、签署入口 | 清理乱码、优化状态文案、补全移动交互体验 | P0 |
| 移动端签署 | 半完成 | 已接真实合同读取和签署动作接口 | 将手写 / 输入 / 上传签名变成真实持久化能力，补全证据闭环 | P0 |
| 后台首页与统计 | 已完成主体建设 | 统计页和分析页已具备真实 API 与管理员校验 | 继续核对统计口径与运营定义的一致性 | P1 |
| 后台用户管理 | 已完成基础能力 | 用户管理 API 存在，后台有用户页 | 建议继续验收筛选、分页、编辑和审计细节 | P1 |
| 后台订阅管理 | 已完成主体建设 | 订阅列表、支付记录、计划修改、用户元数据同步、审计已具备 | 继续做业务验收、补异常测试 | P1 |
| 后台广告位管理 | 已完成主体建设 | 广告位列表、创建、编辑、删除、趋势统计已具备 | 继续做数据口径验收，清理乱码文案 | P1 |
| 后台审计 | 已完成主体建设 | 审计日志、摘要告警、CSV/JSON 导出、管理员权限拒绝记录已具备 | 继续接入更多关键动作、完善告警策略 | P1 |
| 后台版本 / 上传 | 已完成基础设施 | API 已存在，后台版本页已存在 | 建议补页面级完整验收与文件流测试 | P2 |
| 数据统一模型 | 已完成基础设施 | 统一模型、CloudBase/Supabase 双实现、初始化脚本、SQL 脚本已具备 | 继续清理重复 adapter / store / 历史兼容路径 | P1 |
| 质量门禁 | 半完成 | 已恢复 ESLint 构建门禁，未再看到 `ignoreDuringBuilds` | 关闭 `typescript.ignoreBuildErrors`，逐步清理类型错误 | P0 |
| 自动化测试 | 半完成 | 已有 4 个治理 / 主线测试文件 | 增加合同、支付、企业资料、后台主链路测试 | P0 |
| 遗留治理 | 半完成 | legacy 路由已通过 redirect 收口，`__MACOSX` 已清理 | 清理乱码、根目录历史产物、兼容逻辑和注释噪音 | P1 |

---

## 7. 建议的下一阶段实施顺序

### P0：优先立即处理

1. 移动端乱码与文案修复。
2. 移动端签署从演示式 UI 升级为真实签名持久化闭环。
3. 关闭 `typescript.ignoreBuildErrors` 前先清理主要类型问题。
4. 补合同、支付、企业资料、后台主链路测试。

### P1：主线收口

1. 继续清理历史兼容路由和重复 store / adapter。
2. 做后台 users / stats / analytics / ads / subscriptions 的口径验收。
3. 统一支付、后台、移动端和账户中心中的乱码与中英文不一致。
4. 强化后台审计与异常告警覆盖面。

### P2：体验优化

1. 官网和 Dashboard 细节体验优化。
2. 模板中心更深入的治理能力。
3. 版本与上传管理的页面级验收。
4. 仓库正式文档与阶段性报告归档整理。

---

## 8. 最终结论

基于当前代码，本项目已经不是“功能规划中”的状态，而是一个已经完成主产品骨架和多数核心业务闭环的合同平台。

当前最重要的工作重点应从“继续堆页面”转向：

- 收口移动端。
- 清理乱码和历史残留。
- 恢复 TypeScript 质量门禁。
- 补主链路测试。
- 做后台真实数据口径验收。

如果按工程阶段来判断，当前项目更像：

**功能主体已完成，正在进入交付前治理与收口阶段。**
