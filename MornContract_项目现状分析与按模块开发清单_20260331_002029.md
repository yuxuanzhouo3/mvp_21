# MornContract 项目现状分析与按模块开发清单

- 生成时间：2026-03-31 00:20:29 +08:00
- 分析方式：以当前项目代码为准，不以旧需求文档为准
- 当前工作区：`E:\xiangmu\code\new`

## 1. 项目定位：从当前代码反推，本项目要完成什么

结合 `app`、`components`、`lib/data`、`app/api`、支付与后台目录，可以明确看出当前项目不是单点页面站，而是一个围绕“合同起草、企业资料、电子签署、支付订阅、后台治理”的完整 SaaS 产品，目标能力包括：

1. 面向 C 端 / 小团队 / 企业用户的在线合同生成与管理平台。
2. 支持从模板、导入文本、截图/OCR、AI 分析等入口创建合同。
3. 支持合同列表、检索、筛选、排序、归档、详情查看与导出。
4. 支持电子签署流程，包括发起签署、双方确认、提醒、证据留存、签署后电子版留存。
5. 支持用户账号体系、资料设置、安全设置、会话管理、通知偏好、会员状态展示。
6. 支持企业资料管理，多主体切换，营业执照与聊天截图 OCR 识别。
7. 支持支付、订阅、账单、支付状态跟踪及地区化支付能力。
8. 支持控制台 Dashboard、模板中心、团队协作、Billing 视图。
9. 支持后台管理，包括用户、统计、分析、广告位、订阅、版本、上传、审计。
10. 支持国内版与国际版的双区域基础设施，代码中已经同时接入 CloudBase 与 Supabase 依赖与初始化能力。

## 2. 当前代码里已经完成了什么

以下结论以当前代码为依据，代表“已经存在真实页面/接口/数据层支撑”，不等于每个模块都已完全验收。

### 2.1 官网与公开页

已完成较完整的官网/公开内容层，适合作为产品对外入口：

- 首页、功能页、价格页、使用说明、隐私政策、条款、联系页、下载页已具备。
- 说明项目已经不只是后台壳子，而是完整产品前台。

### 2.2 认证与账号体系

认证能力已经明显不是 demo：

- `app/auth/page.tsx` 已承载登录、注册、OTP、忘记密码等流程。
- `app/api/auth/*` 下存在较完整认证接口族。
- 文档与环境变量中已出现 `APP_REGION`、`NEXT_PUBLIC_APP_REGION`、Supabase 相关变量，说明区域切换已进入工程层。

可判定为“认证主链路基本具备”。

### 2.3 用户资料与设置增强

这一块在当前代码里完成度很高，已经覆盖用户提出的多项 P1 能力：

- `components/account/account-control-center.tsx` 已统一管理资料中心。
- 已有头像上传与自动压缩能力。
- 已有通知偏好 / 邮件偏好管理。
- 已有安全设置项。
- 已有会话管理与“撤销其它会话”能力。
- 已有会员状态统一展示，组件中直接接入会员到期/订阅信息。

结论：用户资料与设置增强，当前代码里已经基本落地。

### 2.4 企业资料增强

企业资料模块已经不再只是静态页：

- `app/companies/page.tsx` 已实现企业资料列表与管理。
- 支持多企业资料存在与切换。
- 支持新增、删除、维护企业资料。
- 支持 OCR 上传识别。
- OCR 源已支持营业执照、微信截图、飞书截图。
- 已有 OCR 失败重试计数。
- 上传前压缩策略已存在。
- `app/api/company-info/route.ts`、`app/api/ocr/business-license/route.ts` 已提供接口支撑。
- `lib/data/company-profile-store.ts` 已具备企业资料存储层。

结论：企业资料增强的大部分核心能力已经具备，属于“已完成主体、仍需验收与细节打磨”。

### 2.5 合同创建主流程

合同创建主链路已经非常明确，且已做路由收口：

- 主创建流程集中在 `app/create/page.tsx`、`app/create/import/page.tsx`、`app/create/analyze/page.tsx`、`app/create/edit/page.tsx`。
- 支持模板、导入、分析、编辑等多步创建体验。
- `app/api/contracts/analyze`、`generate`、`ai-chat` 已存在。
- `app/contracts/create/page.tsx`、`app/contracts/new/page.tsx`、`app/contracts/upload-template/page.tsx` 已被 `lib/contracts/legacy-entrypoints.ts` 收口到统一主流程。

结论：合同创建的 canonical flow 已建立，legacy 入口仍在，但已不再是并行主流程。

### 2.6 合同管理增强

合同中心已经具备较完整业务能力：

- `app/contracts/page.tsx` 已实现合同列表。
- 已支持搜索、筛选、排序。
- 已支持下载。
- 已支持归档 / 取消归档。
- 列表统计已包含归档、签署提醒等信息。

合同详情页也明显进入“最终版”状态：

- `app/contracts/[id]/page.tsx` 已有合同详情展示。
- 支持 HTML / Word / PDF 导出。
- 支持签署发起、发送提醒、双方确认等操作入口。
- 支持签署证据展示。
- 支持最终电子版留存展示。
- 支持版本里程碑与操作日志展示。

配套增强模型也已存在：

- `lib/contracts/enhancements.ts` 中已有归档、提醒、证据、签署流程、最终副本、操作日志等结构与处理逻辑。

结论：合同管理增强在当前代码里已完成主功能闭环，是完成度最高的模块之一。

### 2.7 电子签署增强

电子签署并不是空白：

- 合同详情页已能发起签署。
- 已有发送方确认、对方确认、提醒发送等操作。
- 已有签署证据对象与证据列表。
- 已有签署完成后 final copy 的留存逻辑。
- 相关数据结构与动作处理在 `lib/contracts/enhancements.ts` 中比较完整。

结论：电子签署增强已完成主要业务流，后续重点更偏稳定性、法务细节与多端一致性，而非“从 0 到 1”。

### 2.8 Dashboard 与控制台

Dashboard 已经不是纯静态壳：

- `app/dashboard/page.tsx` 通过 `getDashboardOverview()` 读取概览数据。
- `app/dashboard/templates/page.tsx` 通过 `getDashboardTemplates()` 读取模板数据。
- `app/dashboard/team/page.tsx` 通过 `getDashboardTeamMembers()` 读取团队数据。
- `app/dashboard/billing/page.tsx` 通过 `getDashboardBillingSummary()` 读取账单汇总。
- `lib/data/dashboard-store.ts` 已承接 Overview / Templates / Team / Billing 的数据聚合。

结论：Dashboard 首页和主要二级页已经接入真实数据层，不再是早期静态版。

### 2.9 支付、订阅与账单

支付模块也较完整：

- `app/payment/*` 下已有完整支付与订阅相关页面。
- `app/api/payment/*` 存在支付创建、状态查询、历史、取消、续订、单次支付、Webhook 等接口。
- 依赖中同时包含 Stripe、PayPal、支付宝、微信支付能力，说明产品定位兼顾国际与国内支付。
- `lib/data/billing-store.ts` 已承担支付/订阅数据读取与归一化。

结论：主支付模块完成度较高，Dashboard Billing 只是其控制台视图，不是唯一支付入口。

### 2.10 后台管理与审计

后台模块已经形成真实系统轮廓：

- `app/admin/*` 已覆盖 users、stats、analytics、ads、subscriptions、versions、audit、settings 等页面。
- `app/api/admin/*` 已有对应接口。
- `lib/auth/admin-auth.ts` 表明管理员身份校验已进入代码层。
- `lib/data/admin-management-store.ts`、`admin-insights-store.ts`、`admin-audit-store.ts` 已存在。
- 审计模块已具备汇总、导出、告警摘要等能力。

结论：后台并非“待搭建”，而是“已成型、需继续核口径和验收”的状态。

### 2.11 数据模型统一化基础

数据治理已经开始落地，不只是文档口号：

- `lib/data/unified-models.ts` 已有用户/合同/支付/企业资料的统一归一化逻辑。
- `docs/UNIFIED_DATA_MODEL.md` 已存在。
- `scripts/init-unified-governance.mjs` 已存在。
- `supabase/migrations/*` 已存在迁移脚本。

结论：统一数据模型已进入工程化阶段，尚未完全收口，但基础设施已经具备。

## 3. 当前代码里哪些能力是半完成或仍需继续开发

### 3.1 模板中心是“数据库展示页”，但还不是完整模板闭环

`app/dashboard/templates/page.tsx` 已从数据库读取模板，但当前能力主要是：

- 展示模板卡片。
- 点击“使用模板”跳到创建页。

仍未明显看到：

- 模板详情页。
- 模板启用/停用。
- 模板复制。
- 模板版本管理。
- 模板后台治理闭环。

结论：模板中心已脱离静态数据，但仍偏轻量化模板入口。

### 3.2 团队协作页已接入真实数据，但操作闭环未完成

`app/dashboard/team/page.tsx` 虽然已经从数据层读取成员，但：

- 邀请成员按钮是禁用状态。
- 修改角色菜单是禁用状态。
- 移除成员菜单是禁用状态。

结论：团队成员列表是真实的，但邀请、角色管理、团队权限流还没有真正收口。

### 3.3 Dashboard Billing 已接入真实数据，但更偏“汇总视图”

当前 Billing 页有真实汇总数据，但仍主要服务于控制台展示：

- 适合查看订阅概览、账单摘要。
- 不等同于主支付模块那种完整支付操作体验。

结论：Billing 页面已经不是静态，但仍然是“控制台视图”，不是完整支付业务前台。

### 3.4 移动端合同与签署页仍明显是 demo

移动端是当前代码里最明确的“未收口”区域之一：

- `components/mobile/mobile-contract-list.tsx` 仍使用静态数组。
- 页面中仍存在历史乱码文案。
- `components/mobile/mobile-signature.tsx` 仍是前端演示式签署 UI，没有真实后端流程接入。

结论：移动端合同列表和移动端签署仍然偏演示态。

### 3.5 质量门禁还没有完全恢复

`next.config.mjs` 当前仍存在：

- `typescript.ignoreBuildErrors = true`

这意味着：

- 项目虽然能 build。
- 但不能说明类型问题已经清理完成。
- 质量门禁仍然没有完全恢复。

另外，当前 `tests` 目录仅看到：

- `tests/governance-mainline.test.ts`

说明主业务链路测试仍然明显不足。

## 4. 当前代码里已经暴露出的治理问题

### 4.1 旧入口和历史兼容仍然存在

虽然合同创建主流程已收口，但代码里仍保留：

- legacy 合同入口路由。
- 多套历史兼容路径。

这些路径虽然被重定向或统一处理，但仍需要进一步收口和删除无效残留。

### 4.2 仍存在乱码与无效展示内容

移动端示例页面中可以直接看到乱码中文，说明：

- 历史编码问题未清理干净。
- demo 文案与正式文案混存。

### 4.3 mock / demo / legacy 页面尚未完全出清

从移动端页面与部分控制台二级页完成度来看，项目中仍存在：

- 演示型页面。
- 过渡型页面。
- 未完全接到真实操作流的页面。

### 4.4 数据治理在推进，但未到“彻底统一”

虽然已有统一模型、迁移脚本、初始化脚本，但从代码结构判断仍需继续清理：

- 重复 store / adapter / 历史兼容逻辑。
- 后台真实数据口径核对。
- 国内版 CloudBase 与国际版 Supabase 的一致性对齐。

## 5. 结论：当前项目整体完成度判断

如果从“主产品是否已经成型”来看，答案是：**已经成型**。

如果从“是否已经完全收口并达到可稳定交付的产品状态”来看，答案是：**还没有完全收口**。

更准确的判断是：

- 官网、认证、账号中心、企业资料、合同创建、合同管理、电子签署、主支付模块、后台系统，这些主模块已经完成主体建设。
- 模板管理、团队协作闭环、移动端真实化、后台数据口径验收、统一数据治理、历史遗留清理、测试补齐、类型门禁恢复，仍是接下来的核心工作。

## 6. 按模块拆解的开发清单

以下清单按“模块 -> 当前状态 -> 已完成 -> 待开发/待治理 -> 优先级”整理。

### 6.1 官网与公开页

- 当前状态：已完成主体。
- 已完成：
  - 官网主要页面齐备。
  - 适合承载营销、介绍与下载入口。
- 待开发 / 待治理：
  - 检查是否与实际能力同步更新文案。
  - 补足多语言与一致性校对。
- 优先级：中。

### 6.2 认证与账号

- 当前状态：主链路已具备。
- 已完成：
  - 登录、注册、OTP、忘记密码。
  - 多区域环境变量与认证接入基础。
- 待开发 / 待治理：
  - 清理旧 auth 兼容代码。
  - 统一 CN / INTL 两套认证与用户字段映射。
  - 强化异常监控和风控。
- 优先级：高。

### 6.3 用户资料与设置

- 当前状态：已基本完成。
- 已完成：
  - 头像上传压缩。
  - 安全设置。
  - 会话管理。
  - 通知偏好 / 邮件偏好。
  - 会员状态统一展示。
- 待开发 / 待治理：
  - 落实到真实数据库字段与持久化一致性。
  - 接入审计日志。
  - 增加异常提醒与更细权限校验。
- 优先级：高。

### 6.4 企业资料

- 当前状态：主体完成，待验收。
- 已完成：
  - 多主体企业资料列表与管理。
  - OCR 上传识别。
  - OCR 失败重试。
  - 微信 / 飞书截图文字提取入口。
  - 上传压缩策略。
- 待开发 / 待治理：
  - 核对 OCR 失败后的容错与重试上限。
  - 完善国内版 / 国际版存储与 OCR 服务兼容。
  - 增加企业资料主链路测试。
- 优先级：高。

### 6.5 合同创建

- 当前状态：canonical flow 已完成。
- 已完成：
  - `/create` 主流程。
  - 导入、分析、编辑链路。
  - legacy 入口向主流程收口。
- 待开发 / 待治理：
  - 继续移除 legacy/过渡路由。
  - 对齐多入口埋点、权限、参数与来源标记。
  - 补合同创建全链路测试。
- 优先级：高。

### 6.6 合同管理

- 当前状态：高完成度。
- 已完成：
  - 列表、搜索、筛选、排序。
  - 下载与导出。
  - 合同详情页最终版能力。
  - 合同归档。
  - 合同操作日志。
- 待开发 / 待治理：
  - 校对导出文件稳定性与权限控制。
  - 继续统一合同字段规范与版本结构。
  - 增加合同中心端到端测试。
- 优先级：高。

### 6.7 电子签署

- 当前状态：主能力已具备。
- 已完成：
  - 发起签署。
  - 双方确认流程。
  - 签署提醒。
  - 签署证据展示。
  - 签署完成后的电子版留存与查看。
- 待开发 / 待治理：
  - 明确法务合规细节和证据字段完整性。
  - 强化签署异常追踪、失败告警与审计导出。
  - 与移动端签署流程统一。
- 优先级：高。

### 6.8 Dashboard 首页

- 当前状态：已接入真实数据。
- 已完成：
  - Overview 数据读取。
  - 控制台核心概览能力。
- 待开发 / 待治理：
  - 核对首页统计卡片口径与后台统计一致性。
  - 核对最近活动与审计/操作日志统一口径。
- 优先级：高。

### 6.9 模板中心

- 当前状态：半完成。
- 已完成：
  - 模板列表读取。
  - 模板卡片展示。
  - 从模板跳转创建合同。
- 待开发 / 待治理：
  - 模板详情页。
  - 模板启用/停用。
  - 模板复制。
  - 模板后台管理与权限流。
  - 模板版本与审计。
- 优先级：中高。

### 6.10 团队协作

- 当前状态：半完成。
- 已完成：
  - 团队成员列表真实读取。
  - 角色与状态展示。
- 待开发 / 待治理：
  - 邀请成员。
  - 修改角色。
  - 移除成员。
  - 团队权限与资源隔离。
  - 团队审计日志。
- 优先级：中高。

### 6.11 Billing / 支付 / 订阅

- 当前状态：主支付模块较完整，控制台 Billing 待进一步统一。
- 已完成：
  - 主支付模块页面与 API。
  - 订阅、历史、取消、续订等能力。
  - Billing 汇总页接入真实数据。
- 待开发 / 待治理：
  - 核对支付 / 订阅统一字段规范。
  - 完成后台订阅管理验收。
  - 对齐 Dashboard Billing 与主支付模块体验。
  - 增加支付主链路测试。
- 优先级：高。

### 6.12 后台管理

- 当前状态：已成型，待验收。
- 已完成：
  - users / stats / analytics / ads / subscriptions / versions / audit / settings 页面与接口。
  - 管理员认证基础。
  - 后台审计数据结构、汇总、导出、告警摘要。
- 待开发 / 待治理：
  - 核对 users / stats / analytics / ads / subscriptions / upload 的真实数据准确性。
  - 强化管理员权限校验。
  - 增加更多关键操作日志。
  - 完成广告位管理与订阅管理验收。
  - 增加关键操作审计导出、追踪与异常告警。
- 优先级：最高。

### 6.13 数据模型统一化

- 当前状态：已启动，未完全收口。
- 已完成：
  - 统一模型文档。
  - 统一归一化代码。
  - 初始化脚本与迁移脚本基础。
- 待开发 / 待治理：
  - 锁定用户统一字段规范。
  - 锁定合同统一字段规范。
  - 锁定支付 / 订阅统一字段规范。
  - 锁定企业资料统一字段规范。
  - 清理重复 store / adapter / 原型 API。
  - 固化迁移脚本和初始化脚本。
  - 明确 CloudBase 与 Supabase 的统一抽象层。
- 优先级：最高。

### 6.14 移动端

- 当前状态：明显未完成。
- 已完成：
  - 已有移动端页面与基础交互壳。
- 待开发 / 待治理：
  - 将静态合同数据改为真实数据读取。
  - 将移动签署改为真实签署流程。
  - 清理乱码和 demo 文案。
  - 与桌面端合同、签署、通知逻辑统一。
- 优先级：中高。

### 6.15 测试与质量门禁

- 当前状态：明显不足。
- 已完成：
  - 治理主线测试已存在。
- 待开发 / 待治理：
  - 增加合同、支付、后台、企业资料主链路测试。
  - 逐步清理类型错误。
  - 关闭 `typescript.ignoreBuildErrors = true`。
  - 清理 lint 警告与不一致实现。
- 优先级：最高。

### 6.16 历史遗留与无效代码治理

- 当前状态：仍需系统清理。
- 已完成：
  - 部分 legacy 路由已被收口。
- 待开发 / 待治理：
  - 移除 `__MACOSX` 等无关残留。
  - 清理 mock / demo / legacy 页面。
  - 清理无效文案。
  - 修复历史乱码与中英文不一致。
  - 标记或移除无用实验性模块。
- 优先级：高。

## 7. 建议的实施顺序

建议按以下顺序推进，而不是平均铺开：

1. 后台数据口径核对 + 管理员权限 + 审计导出告警。
2. 统一数据模型收口，打通 CloudBase / Supabase 的统一读写规范。
3. 关闭 TypeScript 构建忽略，补主链路测试。
4. 清理旧 auth、重复 store / adapter、legacy 路由、mock/demo 页面。
5. 收口模板中心、团队协作、Dashboard Billing 的业务闭环。
6. 最后补移动端真实化。

## 8. 一句话结论

当前项目已经完成了大部分主产品能力，尤其是合同创建、合同管理、电子签署、企业资料、账号设置、支付和后台系统都已经有较强代码基础；接下来最关键的不是“再堆页面”，而是“统一数据模型、核对后台真实口径、补测试、恢复质量门禁、清理历史遗留，并把几个半成品模块彻底收口”。
