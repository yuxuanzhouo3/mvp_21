# MornContract 项目现状分析与按模块开发清单

- 生成时间：2026-04-02 11:29:24 +08:00
- 分析方式：基于当前项目代码结构、页面、API 路由、数据层与测试文件整理，不以旧文档为准
- 项目路径：`E:\xiangmu\code\new`

---

## 1. 项目定位与当前项目要完成的核心功能

从当前代码看，`MornContract` 不是单一的“合同编辑器”，而是一个围绕 **合同生成、签署、存证验真、企业资料、支付订阅、团队协作、后台治理** 形成的完整业务系统，且同时支持：

- 中国区部署：`CloudBase + 微信/支付宝 + 微信登录`
- 国际区部署：`Supabase + Stripe/PayPal + 邮箱/Google 登录`

当前项目要完成的业务目标，按代码能力归纳为：

1. 让用户把聊天文本、聊天截图、AI 对话采集结果转成合同草稿。
2. 让用户在统一创建主线中完成分析、编辑、生成正式合同。
3. 让合同进入真实签署流程，并记录签署证据、提醒、确认和最终电子副本。
4. 让合同及独立上传文档进入文档中心，支持管理、分享、验真、证书导出。
5. 让企业资料、营业执照 OCR、主体切换成为合同生产资料的一部分。
6. 让支付、订阅、账单、会员状态与产品权限闭环。
7. 让团队、模板、控制台和后台治理支撑正式运营。

---

## 2. 当前代码已完成的功能总览

### 2.1 总体判断

- 按“代码结构完成度”估算：**约 80%**
- 按“生产环境可运营度”估算：**约 65% - 70%**

原因：

- 主流程大部分已经有真实页面、接口和数据层。
- 多个旧结论已经变化，例如截图 OCR、文档库、验真、网页签署入口都不再是纯静态演示。
- 但生产可用还依赖外部服务配置、数据库表结构、Webhook、真实支付商户、AI Key、OCR 能力等。

### 2.2 模块进度表

| 模块 | 当前状态 | 估算进度 | 代码判断 |
| --- | --- | ---: | --- |
| 双区域部署与基础架构 | 已完成主体 | 85% | 已有 CN/INTL 切换、认证/数据库/支付能力分区 |
| 用户认证与账户中心 | 已完成主体 | 80% | 已有登录、注册、刷新、状态、资料、会话、安全设置 |
| 合同创建主线 | 已完成主体 | 85% | `/create -> import -> analyze -> edit` 已成型 |
| 文本导入 + 截图 OCR 导入 | 已完成主体 | 80% | 文本、截图、微信截图均可进入主线 |
| 旧版 AI 对话生成入口 | 部分完成 | 70% | 已能创建草稿并回到主线，但仍保留独立入口 |
| 合同详情 / 编辑 / 导出 | 已完成主体 | 85% | 合同详情、HTML/Word/PDF 导出已存在 |
| 签署流程 | 已完成主体 | 80% | 共享签署流程、签名方式、证据留痕已接通 |
| 文档库与验真 | 已完成主体 | 85% | 独立上传、编辑删除、批量整理、分享、验真、证书已接通 |
| 企业资料与营业执照 OCR | 部分完成 | 75% | 企业资料 CRUD、OCR、主体切换已完成，未自动带入合同 |
| 模板 / 团队 / 控制台 | 已完成主体 | 80% | 模板、团队、账单、概览接口和页面已存在 |
| 支付与订阅 | 部分完成 | 75% | 多支付通道、Webhook、状态同步代码已存在，生产闭环待验证 |
| 后台治理 | 部分完成 | 78% | 用户、版本、广告、分析、审计、订阅后台已存在 |
| 自动化测试 | 基础覆盖 | 55% | 已有主线测试，但不算完整端到端测试体系 |

---

## 3. 基于当前代码确认：已经完成的主要功能

## 3.1 双区域部署架构已经成型

代码依据：

- `lib/config/deployment.config.ts`
- `app/api/auth/config/route.ts`

结论：

- 已明确区分 `CN` 与 `INTL` 两个部署区域。
- 已按区域切换认证、数据库和支付提供方。
- 当前项目不是单区域原型，而是带区域分支能力的正式架构。

## 3.2 合同创建主线已经形成统一主流程

代码依据：

- `app/create/page.tsx`
- `components/create/create-contract-screen.tsx`
- `app/create/import/page.tsx`
- `app/api/contracts/analyze/route.ts`
- `app/create/analyze/page.tsx`
- `app/create/edit/page.tsx`

结论：

- 新主线已经明确：选择导入方式 -> 导入内容 -> AI 分析 -> 编辑完善。
- 文本导入是成熟能力。
- 合同创建后会保存草稿、分析结果、版本历史，再进入后续页面。

## 3.3 截图导入与微信截图导入已经打通，不再是“禁用态”

代码依据：

- `components/create/create-contract-screen.tsx`
- `app/create/import/page.tsx`
- `app/api/contracts/import-screenshot/route.ts`
- `lib/ocr/contract-chat.ts`

结论：

- `/create` 第一步里 `text / screenshot / wechat` 三种方式都被标记为可用。
- 导入页支持上传截图、执行 OCR、识别来源、人工修正文本、再进入分析。
- 这部分和此前“截图导入未完成”的判断相比，**当前代码已经升级为真实能力**。

## 3.4 旧版 AI 对话生成入口仍存在，但已部分并入主线

代码依据：

- `app/contracts/ai-generate/page.tsx`
- `app/api/contracts/ai-chat/route.ts`

结论：

- 旧 AI 对话入口仍然保留，说明入口尚未彻底统一。
- 但它不再是完全孤立的旧方案。
- 当前实现会把 AI 对话收集结果转成草稿，再跳回 `/create/analyze` 主线。

## 3.5 合同详情、版本、导出与流程操作已经具备真实业务结构

代码依据：

- `app/contracts/[id]/page.tsx`
- `app/api/contracts/[id]/route.ts`
- `app/api/contracts/[id]/export/route.ts`
- `lib/contracts/format.ts`
- `lib/contracts/enhancements.ts`

结论：

- 合同详情页不是纯展示页，已经集成：
- 版本历史
- 原始来源快照
- 合同正文预览
- HTML / Word / PDF 导出
- 归档、恢复、发起签署、提醒、确认等动作

## 3.6 网页签署入口已经切到真实共享签署流程，不再是旧静态样例

代码依据：

- `app/dashboard/contracts/[id]/sign/page.tsx`
- `components/contracts/contract-sign-flow.tsx`
- `app/mobile/sign/page.tsx`

结论：

- `dashboard` 网页签署页已改为使用 `ContractSignFlow`。
- 同一套签署流程支持：
- 手写签名
- 打字签名
- 上传签名图
- 法律确认勾选
- 签署动作回写合同
- 证据记录与最终副本保留
- 这一块与早期“旧入口静态样例”的结论相比，**当前代码已经明显前进**。

## 3.7 文档库与验真模块已经从静态演示升级为真实模块

代码依据：

- `app/dashboard/documents/page.tsx`
- `components/documents/document-library.tsx`
- `components/documents/document-verification.tsx`
- `components/documents/storage-stats.tsx`
- `lib/data/dashboard-documents-store.ts`
- `lib/data/workspace-documents-store.ts`
- `app/api/dashboard/documents/**`
- `app/api/public/documents/[token]/**`

结论：

- 当前已具备真实能力：
- 合同文档与独立上传文档统一进入文档库
- 独立上传文档支持新增、编辑、删除
- 支持标签、分类、分组
- 支持批量标签管理
- 支持批量分类 / 批量移动分组
- 支持分享链接生成、撤销、有效期
- 支持分享访问次数、最近访问时间统计
- 支持公共验真页面
- 支持验真证书导出

这意味着“文档库与验真主要还是静态演示”的旧结论，**对当前代码已不成立**。

## 3.8 企业资料与营业执照 OCR 已经形成独立可用模块

代码依据：

- `app/companies/page.tsx`
- `app/contracts/company-setup/page.tsx`
- `app/api/company-info/route.ts`
- `app/api/ocr/business-license/route.ts`
- `lib/data/company-profile-store.ts`
- `app/api/profile/route.ts`

结论：

- 已支持企业资料 CRUD。
- 已支持营业执照 OCR。
- 已支持多主体切换，并将 `activeCompanyProfileId` 写入用户资料。
- 已支持独立“引导式企业设置页”。

## 3.9 团队、模板、账单、控制台模块已有真实后端支持

代码依据：

- `lib/data/dashboard-store.ts`
- `app/api/dashboard/overview/route.ts`
- `app/api/dashboard/templates/**`
- `app/api/dashboard/team/**`
- `app/api/dashboard/billing/route.ts`

结论：

- 控制台概览数据由真实合同、支付、订阅记录汇总生成。
- 模板中心支持：
- 默认模板种子
- 新建模板
- 编辑模板
- 复制模板
- 创建模板版本
- 团队中心支持：
- 成员列表
- 邀请成员
- 更新角色
- 更新状态
- 移除成员
- 账单中心支持从支付与订阅数据汇总最近付款记录。

## 3.10 支付与订阅代码体系已经形成

代码依据：

- `app/api/payment/create/route.ts`
- `app/api/payment/status/route.ts`
- `app/api/payment/webhook/**`
- `app/api/payment/onetime/**`
- `lib/payment/adapter.ts`
- `lib/payment/subscription-payment-sync.ts`
- `lib/data/billing-store.ts`

结论：

- 当前项目不是“只有支付页面没有后端”的状态。
- 已有：
- 创建支付
- 支付状态查询
- Webhook
- 订阅同步
- 一次性支付
- 微信 Native 支付
- 支付记录写库
- 但是否“生产可稳定运营”，仍要看商户配置、Webhook、退款/补偿等实际环境。

## 3.11 后台治理模块已经具备真实管理面

代码依据：

- `app/admin/**`
- `app/api/admin/**`
- `lib/data/admin-management-store.ts`
- `lib/data/admin-insights-store.ts`
- `lib/data/admin-audit-store.ts`

结论：

- 当前后台已覆盖：
- 用户管理
- 订阅管理
- 版本管理
- 广告位管理
- 分析统计
- 审计记录
- 文件上传
- 说明项目已经考虑正式运营和管理需求，而不只是用户端。

---

## 4. 当前仍属于“部分完成 / 待闭环”的功能

## 4.1 企业资料尚未自动带入合同创建主线

代码依据：

- `app/companies/page.tsx`
- `app/api/profile/route.ts`
- 全项目搜索 `activeCompanyProfileId`

结论：

- 当前已支持“保存活动企业主体”。
- 但从当前代码搜索结果看，活动主体尚未自动注入 `/create` 主线的甲方信息。
- 这块仍是下一阶段高优先级整合项。

## 4.2 旧入口仍未完全收敛

表现：

- `/contracts/ai-generate` 仍然保留。
- `/create` 与 `/contracts/ai-generate` 仍是两个入口。

结论：

- 逻辑上已经部分统一。
- 但产品入口层面仍存在重复路径，后续仍应收口。

## 4.3 模板中心是真实模块，但默认模板内容仍偏种子数据

代码依据：

- `lib/data/dashboard-store.ts` 中 `DEFAULT_TEMPLATE_SEED`

结论：

- 模板管理能力是真实的。
- 但默认公共模板内容仍是示例模板文本，不是成熟模板资产库。

## 4.4 团队邀请更像“系统内成员记录”，不是完整邀请协同闭环

代码依据：

- `lib/data/dashboard-store.ts` 中 `inviteDashboardTeamMember`

结论：

- 当前邀请动作会创建 / 更新 `workspace_members`。
- 但代码中未见真正的站外邮件邀请、邀请码确认、成员接受流程。
- 团队模块可用，但协同闭环还不完整。

## 4.5 支付链路代码完整度较高，但生产闭环仍依赖外部配置

结论：

- 代码层面已具备多支付通道。
- 但生产上仍依赖：
- Stripe / PayPal / WeChat / Alipay 配置
- Webhook 回调
- 商户密钥
- 环境变量
- 数据库表结构

因此支付模块应评估为“代码完成较高，生产稳定度待验证”。

## 4.6 测试覆盖还不够深

代码依据：

- `tests/contract-signflow-mainline.test.ts`
- `tests/dashboard-management-mainline.test.ts`
- `tests/governance-mainline.test.ts`
- `tests/payment-governance-mainline.test.ts`
- `tests/routing-governance-mainline.test.ts`

结论：

- 已有 Jest 测试，说明项目并非完全无测试。
- 但目前仍偏“主线冒烟 / 治理回归”。
- 还缺少更完整的端到端测试、支付回调联调测试、权限测试、异常补偿测试。

---

## 5. 按模块开发清单

## 5.1 P0：建议优先完成

### 模块 A：企业主体与合同创建主线联动

- 将 `activeCompanyProfileId` 自动注入合同创建流程。
- 企业 OCR 结果自动带入甲方信息。
- 合同与企业主体建立查询关系。

### 模块 B：创建入口统一收口

- 明确只保留一个推荐入口。
- 决定 `/contracts/ai-generate` 的最终去向：合并、跳转或下线。
- 统一创建完成后的跳转与版本记录结构。

### 模块 C：支付生产闭环验证

- 按支付方式逐一验证创建、查询、Webhook、订阅同步。
- 补退款、失败补偿、幂等等运营边界。
- 对账单展示和订阅状态做最终校验。

### 模块 D：文档验真后台增强

- 增加分享访问明细日志。
- 增加分享管理后台。
- 增加更细粒度的文档操作审计字段。

## 5.2 P1：建议第二阶段完成

### 模块 E：模板资产化

- 把默认模板种子内容升级为可直接使用的正式模板。
- 增加模板分类策略、审核流、发布规则。

### 模块 F：团队协作闭环

- 增加真正的邀请通知能力。
- 增加成员接受邀请和加入流程。
- 增加更细粒度的成员权限。

### 模块 G：后台治理深化

- 细化分析口径、广告运营规则、审计展示。
- 增加后台关键动作的追责字段。
- 增加更多运营异常恢复工具。

## 5.3 P2：建议第三阶段优化

### 模块 H：历史页面清理与统一

- 清理旧入口与重复页面。
- 统一中英文文案与部分乱码问题。
- 统一命名、跳转和模块边界。

### 模块 I：测试体系升级

- 增加签署、支付、权限、分享验真的端到端测试。
- 增加多区域构建与环境回归测试。

---

## 6. 开发进度报告

### 6.1 当前阶段结论

- 项目已经从“原型演示”进入“主流程基本成型”的阶段。
- 合同创建、签署、文档库、验真、企业资料、支付、后台这些大模块都已经有真实代码，不是空壳。
- 当前最重要的问题不是“有没有功能”，而是“能否把这些能力进一步统一、联动、生产化”。

### 6.2 与旧判断相比，当前代码最明显的变化

1. 截图导入、微信截图导入已经接入创建流程。
2. 文档库与验真已经完成真实数据化，不再是纯静态演示。
3. 网页签署入口已切换到共享真实签署流。
4. 旧 AI 对话入口已可回流到标准创建主线。

### 6.3 当前最值得关注的剩余缺口

1. 企业主体尚未自动参与合同生成。
2. 旧入口仍未彻底统一。
3. 模板默认内容仍偏种子数据。
4. 团队邀请未形成完整协作闭环。
5. 支付模块仍需真实生产环境验证。
6. 测试深度仍不足。

---

## 7. 外部依赖与上线风险

- AI 分析与 OCR 依赖外部模型服务配置。
- 国际区依赖 Supabase。
- 中国区依赖 CloudBase。
- 支付依赖 Stripe / PayPal / WeChat / Alipay 商户配置与 Webhook。
- 文档分享、验真与文件存储依赖存储桶与数据库表结构完整。

结论：

- **代码完成度已经不低。**
- **上线可运营度仍取决于环境配置、数据表、Webhook、商户信息和回归测试。**

---

## 8. 最终结论

基于当前代码，`MornContract` 已经完成了一个较完整的合同 SaaS 主干：

- 有统一创建主线
- 有 OCR 导入
- 有 AI 对话采集
- 有合同详情与导出
- 有真实签署流程
- 有文档库、验真、分享、证书
- 有企业资料和营业执照 OCR
- 有支付订阅
- 有团队、模板、控制台
- 有后台治理

当前项目最适合进入的阶段，不是“从零开发”，而是：

**围绕主线统一、企业资料联动、支付生产闭环、团队协作闭环、后台治理深化进行第二阶段工程化完善。**
