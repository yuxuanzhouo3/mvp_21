# MornContract 项目现状分析与按模块开发清单

- 生成时间：2026-04-02 22:14:33
- 时区：Asia/Shanghai
- 分析方式：基于当前项目代码扫描，不依据旧文档结论
- 扫描范围：`app/`、`app/api/`、`components/`、`lib/`、`tests/`
- 当前代码规模快照：
  - 页面路由约 `57` 个
  - API 路由约 `73` 个
  - 测试文件 `11` 组
- 当前验证状态：
  - `npm test -- --runInBand` 已通过
  - `npm run build` 已通过

---

## 一、项目从当前代码看，实际要完成什么功能

从现有代码结构和业务流转看，本项目并不是单一的“合同生成页面”，而是一套围绕 **AI 合同起草、合同管理、签署流转、订阅支付、后台治理、文档验证与多区域部署** 的完整 SaaS 系统。

### 1. 核心业务目标

项目当前代码实际在实现以下主能力：

1. 用户登录后创建合同草稿
2. 通过文本、截图 OCR、微信截图、AI 引导问答等方式收集合同事实
3. 调用 AI 分析提取关键信息，再生成合同正文
4. 对合同进行编辑、保存、导出、归档、查看版本历史
5. 推进合同签署流程，包括发起签署、发送提醒、双方确认、形成最终副本
6. 在 Dashboard 中管理合同、模板、团队成员、文档资产、账单与签署任务
7. 支持订阅付费、支付确认、Webhook 回调、订阅状态同步
8. 支持管理后台对用户、订阅、广告位、版本、审计、系统设置等进行治理
9. 支持文档验证、证书导出、公开分享验证页
10. 支持中国区 / 国际区双区域部署，分别切换认证、数据库与支付提供商

### 2. 产品定位判断

基于代码现状，可以把项目定位为：

- 一个以 **合同起草与签署** 为核心的 SaaS 平台
- 同时带有 **团队协作、模板中心、文档中心、会员订阅、后台治理** 能力
- 并且已经考虑 **中国区 / 国际区双栈部署**

---

## 二、当前项目总体完成度判断

以下是基于代码闭环程度做的判断，属于**代码现状推断**：

- 核心业务主链完成度：`较高`
- 后台治理与运营支撑完成度：`较高`
- 上线完备度：`中高`
- 工程可交付度：`已达到可构建、可测试、可继续上线打磨的阶段`

### 综合判断

可以将当前项目理解为：

- **核心主流程已经基本打通**
- **绝大多数业务模块已经不再是纯演示页**
- **剩余问题主要集中在上线前闭环、下载分发、发票/账单细节、环境配置与少量交互打磨**

---

## 三、按模块现状分析

## 1. 多区域部署与基础架构

### 代码依据

- `lib/config/deployment.config.ts`
- `lib/config/region.ts`
- `middleware.ts`
- `lib/integrations/supabase*.ts`
- `lib/cloudbase/*`

### 当前现状

1. 已支持 `CN` / `INTL` 两种部署区域
2. 中国区默认走 `CloudBase`
3. 国际区默认走 `Supabase`
4. 支付提供商也按区域区分：
   - 中国区：微信支付、支付宝
   - 国际区：Stripe、PayPal
5. 认证能力按区域切换：
   - 中国区偏微信 / CloudBase
   - 国际区偏 Supabase / Email / OAuth

### 结论

- **这一层已经成型，属于项目的底层架构能力，完成度高**

---

## 2. 认证与账号体系

### 代码依据

- `app/auth/page.tsx`
- `app/api/auth/*`
- `app/api/profile/route.ts`
- `app/profile/page.tsx`

### 当前已完成

1. 登录 / 注册页已实现
2. 支持邮箱密码登录
3. 支持邮箱 OTP 登录
4. 支持找回密码与 OTP 校验后重设密码
5. 中国区接入了微信登录相关代码入口
6. 存在手机号短信验证码登录接口
7. 用户资料页支持查看与更新昵称、头像、手机号
8. 已支持账户会话、安全设置、偏好设置的统一读取/写入结构

### 当前判断

- **账号体系已具备正式业务能力**
- **第三方登录是否可用，仍依赖实际环境变量与第三方配置是否补齐**

### 结论

- **模块状态：基本完成**

---

## 3. 合同创建与 AI 主流程

### 代码依据

- `components/create/create-contract-screen.tsx`
- `app/create/page.tsx`
- `app/create/import/page.tsx`
- `app/create/analyze/page.tsx`
- `app/create/edit/page.tsx`
- `app/api/contracts/analyze/route.ts`
- `app/api/contracts/generate/route.ts`
- `app/api/contracts/route.ts`

### 当前已完成

1. 创建入口已统一收敛到 `/create`
2. 已支持多种合同导入方式：
   - 直接粘贴对话文本
   - 上传截图走 OCR
   - 导入微信截图
   - AI 引导式问答
3. 导入内容后会先走 `/api/contracts/analyze`
4. 分析结果会保存成用户草稿合同
5. 分析页允许用户确认关键字段后再调用 `/api/contracts/generate`
6. 生成结果会进入编辑页继续修改
7. 编辑页支持保存、复制、导出
8. 草稿的版本历史、来源方式、活跃企业主体快照等元数据已保留

### 当前关键变化

相比之前未闭环的状态，当前代码已经看到：

1. `/create` 页面已接收 `templateId`
2. `/create/import` 会把 `templateId` 继续透传到分析流程
3. `/create/analyze` 会把 `templateId` 传给生成接口
4. `/api/contracts/generate` 会根据 `templateId` 回查模板内容再参与生成

### 结论

- **模板驱动的合同生成主流程现在已经打通**
- **合同创建主链属于当前项目最完整的模块之一**

### 模块状态

- **已完成**

---

## 4. 合同管理与签署流转

### 代码依据

- `app/contracts/page.tsx`
- `app/contracts/[id]/page.tsx`
- `lib/contracts/enhancements.ts`
- `tests/contract-signflow-mainline.test.ts`
- `app/dashboard/signatures/page.tsx`
- `components/contracts/contract-sign-flow.tsx`

### 当前已完成

1. 合同列表页支持查询、筛选、排序
2. 支持删除、下载、归档 / 取消归档
3. 合同详情页支持：
   - 查看正文
   - 下载 HTML / Word
   - 导出 PDF
   - 查看版本历史
4. 签署流转动作已具备：
   - 发起签署
   - 发起方确认
   - 对方确认
   - 发送提醒
   - 生成最终副本
5. `dashboard/signatures` 已不再是静态占位，而是从真实合同数据中汇总签署任务
6. 移动端签署页也已复用统一签署流组件

### 当前不足

1. 合同列表页仍然有部分旧交互使用 `window.alert` / `window.confirm`
2. 这说明功能已完成，但交互体验还没有完全统一到新的 toast 风格

### 结论

- **合同管理与签署流程已经打通**
- **剩余主要是交互一致性优化，不是主流程缺失**

### 模块状态

- **基本完成**

---

## 5. 企业主体资料与营业执照 OCR

### 代码依据

- `app/contracts/company-setup/page.tsx`
- `app/api/ocr/business-license/route.ts`
- `lib/ocr/business-license.ts`

### 当前已完成

1. 企业资料页面已实现
2. 支持填写企业名称、统一社会信用代码、法人、联系人、地址等字段
3. 支持营业执照 OCR 接口
4. OCR 识别结果可回填企业资料
5. 活跃企业主体可写回后续合同草稿上下文

### 结论

- **该模块已具备正式业务价值**

### 模块状态

- **已完成**

---

## 6. Dashboard 工作台

### 代码依据

- `app/dashboard/page.tsx`
- `app/api/dashboard/overview/route.ts`
- `app/dashboard/contracts/*`
- `app/dashboard/documents/*`
- `app/dashboard/templates/page.tsx`
- `app/dashboard/team/page.tsx`
- `app/dashboard/billing/page.tsx`
- `app/dashboard/signatures/page.tsx`

### 当前已完成

1. 总览页支持统计数据与最近活动
2. 合同页支持统一进入新建合同主流程
3. 文档中心支持：
   - 加载文档库
   - 上传文档
   - 批量打标签
   - 归类组织
   - 下载文档
   - 验证文档
   - 生成分享链接
   - 撤销分享链接
   - 生成证书文件
4. 模板中心支持：
   - 新建模板
   - 编辑模板
   - 复制模板
   - 创建模板版本
   - 启停模板状态
   - 使用模板发起合同创建
5. 团队中心支持：
   - 邀请成员
   - 角色管理
   - 状态管理
   - 移除成员
   - 邀请链接复制 / 打开
6. 账单页支持展示订阅摘要、最近支付、历史账单入口
7. 签署页支持从真实合同签署元数据汇总待办

### 当前不足

1. Dashboard 账单页偏“汇总视图”，真正支付变更仍跳回主支付中心
2. 发票下载并未真正落地：
   - `app/api/payment/history/route.ts` 返回中 `invoiceUrl` 仍为 `null`

### 结论

- **Dashboard 已从演示控制台升级成真实工作台**
- **账单细节仍有一部分待完善**

### 模块状态

- **基本完成**

---

## 7. 支付、订阅与 Webhook

### 代码依据

- `app/payment/page.tsx`
- `app/payment/success/page.tsx`
- `app/payment/cancel/page.tsx`
- `app/payment/redirect/page.tsx`
- `app/payment/wechat-qrcode/page.tsx`
- `app/api/payment/*`
- `lib/payment/subscription-payment-sync.ts`
- `tests/payment-confirm-route.test.ts`
- `tests/payment-webhook-routes.test.ts`
- `tests/payment-governance-mainline.test.ts`

### 当前已完成

1. 已有完整支付中心页面
2. 支持套餐选择、支付发起、支付结果跳转
3. 已有支付创建接口
4. 已有支付确认接口
5. 已有支付继续、取消、状态、历史接口
6. 已有订阅支付成功后的订阅同步逻辑
7. 已有 Stripe / PayPal / 微信 / 支付宝等多渠道支付处理代码
8. 已有多类 Webhook 路由
9. Webhook 已纳入统一处理与测试覆盖

### 当前新增闭环

1. 支付失败通知已不只是“入队”
2. 当前代码中已经存在：
   - 失败通知持久化存储
   - 派发器
   - 管理后台手动触发派发接口
   - 后台系统设置页的立即补偿触发入口

### 当前不足

1. 发票下载链路尚未落地
2. 第三方支付是否能在目标环境完整跑通，仍取决于真实商户配置、密钥和回调环境

### 结论

- **支付与订阅主链已基本形成正式闭环**
- **支付失败通知能力相比此前已明显补齐**

### 模块状态

- **基本完成**

---

## 8. 管理后台

### 代码依据

- `app/admin/page.tsx`
- `app/admin/analytics/page.tsx`
- `app/admin/users/page.tsx`
- `app/admin/settings/page.tsx`
- `app/admin/ads/page.tsx`
- `app/admin/versions/page.tsx`
- `app/admin/subscriptions/page.tsx`
- `app/admin/audit/page.tsx`
- `app/api/admin/*`

### 当前已完成

1. 后台总览可加载真实统计
2. 分析页可查看后台指标
3. 用户页支持查询、导出、禁用 / 恢复等管理动作
4. 系统设置页已经接入真实 API：
   - 可读取设置
   - 可保存设置
   - 设置已持久化到 CloudBase / Supabase
5. 广告位页支持新建、编辑、删除、查看趋势数据
6. 版本页支持上传安装包、创建版本、启停版本、删除版本
7. 订阅页支持查看订阅 / 支付记录、搜索、筛选、编辑订阅
8. 审计页支持：
   - 状态筛选
   - 警报汇总
   - CSV / JSON 导出
9. 后台支付失败通知派发支持手动触发

### 当前结论

- **管理后台已经不是演示级 UI，而是实管理后台**
- **后台治理能力已经覆盖用户、订阅、广告、版本、审计、系统设置**

### 模块状态

- **已完成 / 基本完成**

---

## 9. 文档验证、公开分享与证书

### 代码依据

- `app/api/dashboard/documents/[id]/verify/route.ts`
- `app/api/dashboard/documents/[id]/share/route.ts`
- `app/api/dashboard/documents/[id]/certificate/route.ts`
- `app/api/public/documents/[token]/verify/route.ts`
- `app/api/public/documents/[token]/certificate/route.ts`
- `app/verify/document/[token]/page.tsx`

### 当前已完成

1. 已支持文档验证数据读取
2. 已支持分享验证链接
3. 已支持撤销分享链接
4. 已支持私有证书导出
5. 已支持公开验证页
6. 已支持公开证书导出

### 结论

- **文档验证与公开证明链路已经打通**

### 模块状态

- **已完成**

---

## 10. 移动端能力

### 代码依据

- `app/mobile/contracts/page.tsx`
- `components/mobile/mobile-contract-list.tsx`
- `app/mobile/sign/page.tsx`
- `components/mobile/mobile-signature.tsx`

### 当前已完成

1. 移动端合同列表已实现
2. 支持移动端查看合同
3. 支持移动端下载合同
4. 支持移动端分享合同链接
5. 支持移动端进入签署页
6. 移动端签署复用统一签署流组件

### 结论

- **移动端不是空壳，已经接入真实合同与签署能力**

### 模块状态

- **基本完成**

---

## 11. 下载中心与版本分发

### 代码依据

- `app/download/page.tsx`
- `lib/config/download.config.ts`
- `app/admin/versions/page.tsx`

### 当前现状

1. 后台版本管理页已经存在并可管理版本记录
2. 公开下载页也已经存在
3. 但下载页依然主要依赖 `lib/config/download.config.ts` 中的静态配置
4. 配置中仍有明显占位信息，例如：
   - `cloud://your-bucket/...`
   - GitHub 示例下载地址
   - `multigpt-*` 文件命名残留
5. 下载页中国区分支会跳转 `/api/downloads?...`
6. 当前项目中**没有发现 `app/api/downloads` 路由实现**

### 结论

- **下载中心 / 版本分发是当前最明确的未闭环模块之一**
- **后台版本管理已完成，但未真正反向驱动公共下载页**

### 模块状态

- **部分完成**

---

## 四、当前已经完成的功能汇总

如果只从“是否算完成主要业务能力”来判断，当前已经完成的重点包括：

1. 双区域部署架构
2. 登录 / 注册 / OTP / 找回密码 / 资料维护
3. AI 合同创建主流程
4. 模板驱动的合同生成闭环
5. 合同列表、详情、导出、归档
6. 合同签署流、提醒、完成副本
7. 企业主体资料与营业执照 OCR
8. Dashboard 总览、合同、模板、团队、文档、账单、签署
9. 支付创建、确认、Webhook、订阅同步
10. 支付失败通知队列 + 派发器 + 后台手动触发
11. 后台用户、版本、广告位、订阅、审计、系统设置
12. 文档验证、公开分享与证书导出
13. 移动端合同查看与移动签署入口
14. 构建与测试主链路

---

## 五、当前未完成 / 半成品 / 待继续闭环部分

这一部分是当前代码里最值得继续推进的内容。

### 1. 下载中心与版本管理未真正打通

这是当前代码里最清晰的缺口。

现状：

1. 后台 `版本管理` 已完成
2. 公共 `下载页` 已存在
3. 但下载页仍读取静态配置文件
4. 国内下载路径引用 `/api/downloads`，代码中未发现该路由
5. 下载配置中仍保留占位 bucket、示例 URL、历史命名残留

结论：

- **后台版本管理做出来了，但“版本管理 -> 对外下载分发”闭环还没真正完成**

### 2. 发票 / 账单明细链路未完整落地

现状：

1. 支付历史接口已实现
2. Dashboard 账单页已能展示摘要与记录
3. 但 `invoiceUrl` 当前固定返回 `null`

结论：

- **账单与支付记录可看，但发票能力尚未落地**

### 3. 第三方登录与支付仍依赖真实环境配置

现状：

1. 代码入口与流程已存在
2. 但 OAuth / 微信登录 / 支付商户能力是否真正可用，仍依赖环境变量与外部平台配置

结论：

- **代码能力已具备，但生产可用性还取决于部署配置**

### 4. 少量旧页面交互规范仍未完全统一

现状：

1. 后台大部分页面已统一为 toast / 空状态 / 成功失败反馈
2. 但如合同列表页仍保留 `window.alert` / `window.confirm` 的旧交互

结论：

- **不是功能缺失，而是前台交互一致性还可继续优化**

### 5. 下载页与部分非核心文件仍有历史示例痕迹

现状：

1. 下载页更新日志是硬编码日期
2. 下载配置仍有示例链接与历史命名
3. 这类内容不影响核心主链，但不适合直接作为正式上线版本

结论：

- **属于上线前清理项，不是业务主阻塞项**

---

## 六、项目按模块开发清单

| 模块 | 当前状态 | 说明 | 下一步 |
| --- | --- | --- | --- |
| 多区域部署 | 已完成 | CN / INTL 双栈已成型 | 继续维护环境基线 |
| 认证与账号 | 基本完成 | 邮箱、OTP、资料维护可用 | 补齐生产环境 OAuth 配置 |
| 企业资料与 OCR | 已完成 | 营业执照 OCR + 企业资料回填 | 增加更多识别容错 |
| AI 合同创建主流程 | 已完成 | 导入、分析、生成、编辑闭环已打通 | 扩展更多模板与异常测试 |
| 模板中心 | 已完成 | 创建、编辑、复制、版本、使用模板已可用 | 可继续做权限与运营增强 |
| 合同管理 | 基本完成 | 列表、详情、导出、归档均可用 | 统一旧交互风格 |
| 签署流程 | 基本完成 | 发起、提醒、确认、完成副本已打通 | 可补更多签署态测试 |
| Dashboard 工作台 | 基本完成 | 总览、模板、团队、文档、账单、签署均可用 | 继续做体验打磨 |
| 文档中心与验证 | 已完成 | 上传、分享、验证、证书导出已可用 | 扩展批量能力 |
| 支付与订阅 | 基本完成 | 创建、确认、Webhook、订阅同步已可用 | 补发票与运营细节 |
| 支付失败通知 | 基本完成 | 队列、派发器、后台触发已接入 | 继续完善监控与运营报表 |
| 管理后台 | 基本完成 | 用户、版本、广告、订阅、审计、设置已可用 | 扩展批量操作与运营能力 |
| 移动端 | 基本完成 | 合同列表、查看、下载、签署入口已可用 | 继续优化移动端细节 |
| 下载中心 / 分发 | 部分完成 | 后台版本有了，但公共下载闭环没打通 | 优先补 `/api/downloads` 与版本联动 |
| 发票能力 | 待补齐 | 账单有记录，发票未落地 | 增加 invoice 生成 / 下载 |
| 工程质量 | 基本完成 | 构建与测试通过 | 继续处理依赖与非阻塞告警 |

---

## 七、开发进度报告

## 1. 进度结论

基于当前代码，我更倾向于这样判断：

### 核心业务进度

- **AI 合同创建 + 合同管理 + 签署 + 模板 + Dashboard 核心工作台：已进入可交付阶段**

### 平台治理进度

- **后台治理、用户管理、订阅管理、审计、系统设置：已进入可运营阶段**

### 上线完备度

- **距离“完全上线就绪”还差少量闭环**

主要差在：

1. 下载中心 / 版本分发未打通
2. 发票能力未落地
3. 第三方登录 / 支付需要真实生产配置联调
4. 少量前台交互与运营内容还可继续统一

## 2. 当前阶段判断

项目当前最准确的阶段不是“刚开始做”，而是：

- **核心系统已做成**
- **后台治理已基本成型**
- **正在从“可用”走向“可上线、可运营、可持续维护”**

---

## 八、建议的下一阶段优先级

### P0：优先补齐上线闭环

1. 补齐公共下载分发能力
2. 将后台版本管理与下载页、下载接口联动
3. 补发票 / 发票下载能力
4. 完成第三方登录与支付的生产环境配置联调

### P1：统一上线前体验

1. 统一前台旧页面成功 / 失败反馈风格
2. 清理下载页与非核心页面中的示例化信息
3. 继续收口少量历史交互与运营表达

### P2：工程与运营增强

1. 增加更多异常路径测试
2. 扩展后台批量操作能力
3. 继续维护依赖与构建基线

---

## 九、最终结论

从当前代码而不是旧文档来看，MornContract 已经不是“只有页面外壳”的项目，而是一套：

- 以 **AI 合同创建与签署** 为核心
- 具备 **模板中心、团队协作、文档验证、支付订阅、后台治理**
- 同时支持 **中国区 / 国际区双栈部署**

的较完整业务系统。

当前最值得继续推进的，不再是“把主功能做出来”，而是：

1. 补齐下载分发闭环
2. 补齐发票能力
3. 完成生产环境级联调
4. 做最后一轮上线前统一打磨

