# MornContract 项目现状分析与按模块开发清单

- 生成时间：2026-04-02 14:14:33（Asia/Shanghai）
- 分析方式：基于当前项目代码实际扫描、测试结果与构建结果整理，不沿用旧文档结论
- 项目路径：`E:\xiangmu\code\new`

## 一、项目定位与目标判断

从当前代码来看，本项目并不是一个单一的“合同生成页面”，而是一个较完整的 **AI 合同平台 / 数字合同工作台**，目标是覆盖以下完整业务闭环：

1. 用户注册、登录、身份验证与账号安全管理
2. 从文本、截图、微信聊天、AI 引导问答中采集合同信息
3. AI 分析谈判内容，提取关键条款并生成合同草稿
4. 对合同进行编辑、版本留痕、导出与下载
5. 发起签署、记录签署证据、提醒对方、生成最终电子版
6. 管理公司主体资料，并将公司主体信息自动带入合同草稿
7. 管理文档资产、分享验真链接、生成验真证书
8. 支持订阅付费、一次性支付、支付回调、账单查询
9. 支持团队成员邀请、角色管理、工作区协作
10. 提供管理后台，覆盖用户、订阅、广告、版本、审计、统计分析
11. 支持中国区与国际区双部署形态

综合判断：**本项目的目标是做一个“面向中国区/国际区双环境部署的 AI 合同生成 + 签署 + 文档验真 + 订阅付费 + 团队协作 + 管理后台”的完整平台。**

## 二、技术与架构现状

| 项目 | 当前情况 |
| --- | --- |
| 前端框架 | Next.js 15.2.4 + React 19 + TypeScript |
| 路由模式 | App Router |
| UI 体系 | Tailwind CSS + Radix UI 组件体系 |
| 状态/交互 | React Hooks，本地状态为主 |
| 中国区数据层 | CloudBase / CloudBase 集合与存储 |
| 国际区数据层 | Supabase（Auth / DB / Storage） |
| AI 能力 | OpenAI / DashScope(Qwen) 双通道 |
| OCR 能力 | 营业执照 OCR、合同聊天截图 OCR |
| 支付能力 | 微信支付、支付宝、Stripe、PayPal |
| 鉴权形态 | CloudBase Auth / Supabase Auth / JWT / Refresh Token |
| 安全能力 | 中间件治理、限流、CSRF、密码安全、账号锁定、审计 |
| 测试框架 | Jest |

### 架构特征

1. 存在明显的 **CN / INTL 双区域部署设计**，并非单一环境项目。
2. 大量数据读写都通过 `lib/data/*` 做了统一封装，说明项目已经从纯页面开发进入了实际业务分层阶段。
3. 合同、支付、团队、文档、后台治理等模块之间已经形成较完整的 API + Store + Page 结构，而不是零散 Demo。

## 三、代码规模与验证结果

### 1. 代码路由规模

- 页面路由数量：57 个
- API 路由数量：71 个
- 测试文件数量：11 个

### 2. 已执行验证

- `npm test -- --runInBand`：**11 个测试套件全部通过，30 个测试全部通过**
- `npm run build`：**生产构建通过**

### 3. 构建期观察到的问题

当前构建虽然成功，但仍有一些质量项未收口：

1. 多个页面存在 `react-hooks/exhaustive-deps` 告警
2. `app/payment/wechat-qrcode/page.tsx` 仍使用 `<img>`，有 Next.js 图片优化告警
3. 这类问题不阻塞构建，但说明项目还没有完全进入“收尾上线”状态

## 四、当前已完成功能盘点

以下内容是根据当前代码判断出的 **真实已落地功能**。

### 1. 公共官网与入口模块

已完成：

1. 首页、定价页、功能介绍页、使用流程页、下载页、隐私政策、服务条款等公共页面已具备
2. 存在公共壳层组件与营销组件，如 Hero、CTA、Features、Testimonials 等
3. 旧入口 `/contracts/ai-generate` 已重定向到新建流 `/create/ai-chat`

判断：

- 官网展示层基本齐备
- `contact` 页面目前仍使用示例联系方式，偏演示内容，未见后台配置接入

### 2. 认证与账号体系

已完成：

1. 邮箱注册与登录
2. 手机验证码登录
3. 中国区微信登录
4. 登录状态查询、刷新、退出登录
5. 用户资料读取与更新
6. 前端认证状态管理、Token 预加载、刷新 Token 管理
7. 账号锁定、密码强度校验、登录安全日志
8. `auth/me`、`auth/status`、`auth/update`、`auth/unlock` 等接口均已存在

判断：

- 认证体系不是空壳，已经具备实际业务能力
- 且存在中国区与国际区不同认证实现

### 3. 合同创建主链路

已完成：

1. `/create` 提供创建入口选择
2. 支持从文本粘贴导入合同谈判内容
3. 支持上传截图进行 OCR 识别
4. 支持微信聊天截图导入
5. 支持 AI 引导式问答采集需求
6. `/create/analyze` 可对 AI 提取的关键条款进行确认/修正
7. `/create/edit` 提供富文本编辑、保存、版本留痕、下载导出
8. `contracts` API 已支持列表、详情、创建、更新、删除
9. 合同数据已统一映射到 `UnifiedContractRecord`

判断：

- 合同创建主流程已经成型，是当前项目最完整的业务主线之一

### 4. AI 分析与生成能力

已完成：

1. 根据对话内容预判合同类型
2. 针对合同类型选择不同“专家角色”分析
3. 生成结构化分析结果：合同类型、甲乙方、关键条款、风险、缺失信息等
4. 根据分析结果生成正式合同结构
5. 中国区与国际区分别可走 DashScope / OpenAI

判断：

- AI 不是单次 prompt 拼接，而是已经形成“预分析 -> 专家分析 -> 合同生成”的链路

### 5. OCR 与企业资料模块

已完成：

1. 营业执照 OCR
2. 合同聊天截图 OCR
3. 公司主体资料向导页
4. 多公司主体管理页
5. 可设置当前激活的公司主体
6. 激活主体信息会自动带入合同分析/草稿上下文

判断：

- 企业资料模块与合同创建链路已经打通，这是项目的一个实际亮点

### 6. 合同中心与合同详情

已完成：

1. 合同列表页支持搜索、筛选、排序
2. 支持归档/取消归档
3. 支持删除合同
4. 支持下载合同
5. 合同详情页支持查看签署状态、最终电子版、证据记录

判断：

- 合同中心已具备“管理后台式”的使用能力，不再只是生成后即丢弃

### 7. 签署流程与电子存证

已完成：

1. 合同签署状态机已实现
2. 支持 `start_signing / confirm_sender / confirm_counterparty / send_reminder`
3. 支持签名采集（输入、绘制、上传等）
4. 支持提醒次数与提醒记录
5. 支持签署证据记录
6. 支持最终电子版留存
7. 提供桌面签署页与移动签署页

测试覆盖：

1. `contract-signflow-mainline.test.ts`
2. `contract-signflow-edgecases.test.ts`
3. `contract-draft-context.test.ts`

判断：

- 这一块已经不是“前端表现层”，而是有明确业务规则与测试保护的

### 8. 文档中心与验真模块

已完成：

1. 工作台文档列表
2. 文档上传
3. 标签、分类、分组管理
4. 批量标签更新与批量归类
5. 文档下载
6. 文档分享链接生成与撤销
7. 文档验真页
8. 公共验真页
9. 验真证书生成
10. 合同文档与上传文档统一进入文档中心视图

判断：

- 文档模块完成度较高，已具备对外分享与验真能力

### 9. 团队协作与工作区邀请

已完成：

1. 团队成员列表
2. 邀请成员
3. 角色管理（owner/admin/member）
4. 成员状态管理（active/invited/suspended）
5. 邀请链接复制与打开
6. 公共邀请预览页
7. 邀请接受接口
8. 邀请过期/撤销/访问计数处理

测试覆盖：

1. `workspace-invites-store.test.ts`
2. `dashboard-management-mainline.test.ts`

判断：

- 团队协作模块已经具备可用的工作区能力

### 10. 模板库模块

已完成：

1. 模板中心页面
2. 默认模板种子自动初始化
3. 模板创建
4. 模板编辑
5. 模板复制
6. 模板归档/启用
7. 模板版本化

判断：

- 模板管理能力本身已成型
- 但与合同创建主流程的最终打通还不完整，详见后文“未完成项”

### 11. 订阅支付与账单模块

已完成：

1. 支付方案页与支付表单
2. 订阅支付创建、继续、确认、取消、状态查询、历史查询
3. 一次性支付创建、确认、回调
4. 微信原生支付二维码页
5. Stripe / PayPal / Alipay / WeChat Webhook 路由
6. 支付成功后订阅同步逻辑
7. Dashboard 账单摘要与支付记录
8. Admin 订阅/支付管理接口

测试覆盖：

1. `payment-governance-mainline.test.ts`
2. `payment-confirm-route.test.ts`
3. `payment-webhook-routes.test.ts`

判断：

- 支付模块已具备“真实业务系统”结构
- 但仍有部分运营/通知补完未完成

### 12. 管理后台模块

已完成：

1. 后台首页统计总览
2. 用户管理
3. 用户详情弹窗与封禁/解封
4. 订阅/支付后台管理
5. 广告管理
6. 版本管理与安装包上传
7. 审计日志
8. 统计分析页

判断：

- 后台已经不是页面壳子，绝大部分核心运营模块已接上 API

### 13. 账号设置与个人中心

已完成：

1. 统一账户控制中心
2. 个人资料编辑
3. 头像上传压缩
4. 通知偏好设置
5. 安全设置
6. 密码修改
7. 会话列表展示
8. 当前激活公司主体写回用户资料

判断：

- 用户中心已具备真实的配置与保存能力

### 14. 安全、治理与工程基础能力

已完成：

1. 路由中间件
2. 速率限制
3. CSRF
4. 密码安全校验
5. 账号锁定
6. 环境变量校验
7. 健康检查与启动检查
8. 后台审计日志能力

测试覆盖：

1. `governance-mainline.test.ts`
2. `routing-governance-mainline.test.ts`
3. `auth-state-manager.test.ts`

判断：

- 基础治理与安全意识较强，不像早期原型项目

## 五、当前未完成 / 半成品 / 待打通部分

这一部分是本次分析里最重要的结论：**哪些看起来“有页面”，但实际上还不能算完全完成。**

### 1. 模板库与合同生成主流程没有完全打通

现状：

1. 模板中心支持“使用模板”
2. 页面会跳转到 `/create?templateId=xxx`
3. `api/contracts/generate` 也保留了 `templateId` 参数

问题：

1. 当前创建页主流程中，没有看到完整消费 `templateId` 的实现
2. `/create/analyze` 调用生成接口时也没有把模板参数继续传下去

结论：

- **模板管理做出来了，但模板驱动的合同生成闭环还没有完全接上**

### 2. Dashboard 的“签署/待办”页面仍是静态占位

现状：

1. `/dashboard/signatures` 页面存在
2. 但页面中显示的是固定的 `0`、空状态和说明文案
3. 未看到真实数据获取逻辑

结论：

- **签署工作台汇总页仍是占位页，不应算作已完成**

### 3. Admin 系统设置页目前是前端静态演示

现状：

1. `/admin/settings` 页面存在
2. 点击保存仅执行本地 `setTimeout` 模拟保存成功
3. 表单默认值是硬编码，未接 API

结论：

- **后台系统设置模块目前还是演示级 UI，尚未真正落地**

### 4. 支付失败通知链路仍未补完

现状：

1. 支付 Webhook 主处理器已实现
2. 但在 `lib/payment/webhook-handler.ts` 中仍明确保留了 `TODO`
3. 内容是“支付失败通知逻辑未实现”

结论：

- **支付主链路基本可用，但支付失败后的通知、补偿、重试策略仍缺失**

### 5. 构建通过但还有一批前端质量告警未收口

现状：

1. 多个页面存在 Hook 依赖告警
2. 图片优化告警尚未处理

结论：

- **工程质量进入“可交付但未完全打磨”的阶段**

### 6. 个别公共页面仍带有示例/演示内容

现状：

1. `contact` 页仍使用示例邮箱与电话
2. 部分营销型页面内容更偏演示性质

结论：

- **核心业务不受影响，但对外正式上线前需要替换真实运营信息**

## 六、按模块完成度评估

以下进度为基于当前代码的主观工程评估，用于项目管理参考：

| 模块 | 当前状态 | 进度评估 |
| --- | --- | --- |
| 公共官网与入口 | 页面齐全，可展示 | 85% |
| 认证与账号体系 | 真实可用，双区分流 | 85% |
| 合同创建主链路 | 最完整主链路之一 | 85% |
| AI 分析与生成 | 已接模型与专家提示词 | 80% |
| 公司主体与 OCR | 已打通到合同草稿上下文 | 85% |
| 合同中心与详情 | 已可管理、归档、下载 | 80% |
| 签署流程与存证 | 规则完整，测试覆盖较好 | 80% |
| 文档中心与验真 | 完整度较高 | 85% |
| 团队协作与邀请 | 已具备真实工作区能力 | 80% |
| 模板管理 | 管理能力完成，流程联动未闭环 | 65% |
| 支付与订阅 | 真实支付结构已成型 | 75% |
| 管理后台 | 大部分模块可用 | 75% |
| 后台系统设置 | 仅 UI 演示 | 35% |
| Dashboard 签署待办页 | 占位页 | 30% |
| 安全与治理 | 基础较扎实 | 80% |
| 测试与工程质量 | 已可构建、已具测试，但仍需收口 | 75% |

### 综合进度判断

**按当前代码综合评估，项目整体完成度约为 78% 左右。**

这个 78% 的含义是：

1. 核心业务链路已经基本跑通
2. 已经不是原型项目，而是可运行、可构建、可测试的业务系统
3. 但距离“稳定上线并长期运营”仍差若干关键打磨项

## 七、按模块开发清单

以下清单按照“建议优先级”排序。

### P0：应优先补齐

1. 打通“模板中心 -> 合同生成主流程”
2. 完成 `/dashboard/signatures` 的真实数据聚合与待办呈现
3. 完成 `/admin/settings` 的真实持久化 API、读写逻辑与权限控制
4. 补齐支付失败通知、补偿处理、失败重试/人工介入策略
5. 清理当前构建告警，收口前端质量问题

### P1：建议尽快完善

1. 为模板打通更多测试，覆盖模板套用、版本衍生、生成落库
2. 为支付主链路补充更多集成测试，尤其是多支付渠道成功/失败分支
3. 为签署待办页补充提醒策略、按人/按合同/按状态过滤能力
4. 为 Admin Analytics 的“导出报表”按钮补上真实功能
5. 替换公共页面中的示例运营信息

### P2：上线前优化项

1. 统一前端国际化文案与编码格式，避免历史字符串出现展示不一致
2. 补充端到端测试，覆盖创建合同、支付、签署、文档验真、邀请入组
3. 检查移动端 / 微信端路径在真实环境下的交互闭环
4. 清理历史遗留脚本、旧分析文档与演示文件，降低维护噪音

## 八、建议的下一阶段开发顺序

建议按下面顺序推进，而不是平均摊开：

1. 合同模板与生成流程打通
2. Dashboard 签署待办页做成真实业务页面
3. Admin 系统设置从演示页改成正式配置中心
4. 支付失败通知与异常补偿机制补齐
5. 构建告警清理与质量收口
6. 增补关键业务集成测试 / 端到端测试
7. 上线前替换示例运营内容与检查环境配置

## 九、项目现状总结

### 可以明确认定已经完成的部分

1. 核心的“合同创建 -> AI 分析 -> 编辑 -> 存储”主链路
2. 认证体系与用户中心
3. 公司主体资料与 OCR 识别
4. 文档中心与验真
5. 团队邀请与成员管理
6. 大部分支付与后台管理骨架
7. 安全治理与基础工程能力

### 不能算完全完成的部分

1. 模板与合同生成闭环
2. Dashboard 签署待办页
3. Admin 系统设置
4. 支付失败通知与补偿链路
5. 若干工程质量与上线前收尾项

### 一句话结论

**这个项目当前已经完成了大部分核心业务能力，属于“主体功能已成型、可构建可测试、但仍有数个关键模块待打通和收尾”的阶段；如果以正式上线为目标，优先级最高的是模板闭环、签署待办页、后台系统设置、支付失败通知和工程质量收口。**

## 十、附：本次分析依据

本次结论主要基于以下代码事实：

1. `app/` 下页面与 `app/api/` 下接口的实际存在情况
2. `lib/data/*`、`lib/contracts/*`、`lib/payment/*`、`lib/auth/*` 的真实业务实现
3. `tests/` 中已存在并已执行通过的测试
4. `npm test -- --runInBand` 的通过结果
5. `npm run build` 的通过结果与构建告警

